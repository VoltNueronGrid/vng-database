apiVersion: v1
kind: ConfigMap
metadata:
  name: voltnuerongrid-config
data:
  config.yaml: |
    cluster:
      mode: "multi"
    network:
      http_bind: "0.0.0.0:8080"
      sql_bind: "0.0.0.0:5432"
      gossip_bind: "0.0.0.0:7000"
    storage:
      data_dir: "/var/lib/voltnuerongrid"
