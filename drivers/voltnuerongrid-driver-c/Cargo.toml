[package]
name = "vng-driver-c"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "C FFI binding layer for the VoltNueronGrid driver"

[lib]
name = "voltnuerongrid_driver"
crate-type = ["cdylib", "staticlib"]

[dependencies]
