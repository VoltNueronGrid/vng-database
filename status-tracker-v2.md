# VoltNueronGrid DB — Status Tracker v2 (Architecture vs Codebase)

**Purpose:** Reconcile the **design architecture** (`reference/voltnuerongrid-db-design.md`, `reference/voltnuerongrid-ws.md`) with **what is actually implemented in Rust today**, call out **gaps** honestly, and provide a **sprint-oriented backlog** by workstream.  
**Companion:** `status_tracker.md` (REQ/WS narrative + evidence links).  
**Last updated:** 2026-04-07 (session 33)  

---

## 0) Answering “Only tests, no Rust?”

**No — development is not “tests only.”** The repository contains substantial production Rust:

| Location | Role | Notes |
|----------|------|--------|
| `services/voltnuerongridd/src/main.rs` | Axum HTTP service | **Very large** single binary: routes for SQL, ingest, store, security/KMS, SRE/DR, failover simulation, cache (Redis-compat HTTP), plugins, autonomous plane, drivers pool, benchmarks, etc. This *is* application development, not “just tests.” |
| `crates/voltnuerongrid-*` | Domain libraries | Multiple **non-trivial** crates (`auth`, `ingest`, `plugins`, `opt`, `store` modules, `sql`, `driver-rust`, …). |
| `tests/kpi/scripts/*.ps1` | Gate/smoke harness | **Integration/contract verification** around the Rust runtime; they do not replace the Rust code. |

**What is true:** much of the **data plane** described in the architecture (Raft metadata clusters, distributed OLTP/OLAP executor fleets, columnar zones, trillion-row proven scale) is **not implemented as separate long-running clusters** yet. A lot of behavior is **scaffolded** (in-memory structures, simulated handoffs, HTTP APIs, contracts) with **heavy test and gate coverage**. So: **lots of Rust + lots of tests**, but **not** “the full distributed HTAP engine as drawn in every diagram.”

---

## 1) Architecture (target) vs implementation (today) — summary

**Target (design doc):** cloud-native HTAP platform — gateways, control plane (catalog, raft, scheduler), data plane (routers, coordinators, OLTP executors, OLAP executors), storage, plugins, drivers, Studio UI, multi-cloud operator.

**Today (codebase):**

- **Implemented well (as scaffolding / single-node runtime):** HTTP API surface, RBAC matrix enforcement paths, ingest connectors (CSV/JSON/Parquet/Excel + chunked/async patterns), ingest outbox/event-bus abstractions, audit sink, plugin manifest lifecycle, distributed **cache manager** (`voltnuerongrid-opt`) + Redis-style HTTP command surface, driver Rust client contracts/pool simulation, SQL **analyze/route/execute** paths with HTAP **routing heuristics** (`HtapQueryRouter`), pessimistic lock scaffold, ACID registry/savepoints/isolation hooks (as coded in service), store **index + constraint** engines, WAL adapter + durability **engine** (in-memory + file adapters where wired), failover **simulation** and replication transport **abstractions**, many SRE endpoints, i18n catalog, benchmark endpoints, concurrency tests — all primarily **in-process** and **single-binary** oriented.

- **Thin or stub crates:** `voltnuerongrid-core`, `voltnuerongrid-meta`, `voltnuerongrid-failover` are **stubs** (crate name constants only). Failover **logic** largely lives in `voltnuerongridd` instead of the dedicated crate.

- **Major gaps vs design:** separate **metadata Raft service**, real **shard coordinators**, **vectorized OLAP execution engine** over columnar storage, durable **row-level MVCC** store matching Postgres-like semantics at scale, **native wire protocol** beyond HTTP for all clients, **Studio UI** in this repo, **production multi-node** consensus and storage, **formal ANSI conformance suite** driving pass/fail, **connector plugins** to real cloud sources beyond contracts, **TDE/TLS termination** as first-class server behavior (much is contract/simulation today), **game-day** automation tied to real clusters.

---

## 2) Crate inventory — maturity

Legend: **DONE** = fit for current scaffold scope; **PARTIAL** = real code, incomplete vs design; **STUB** = placeholder crate; **N/A** = not applicable.

| Crate / binary | Maturity | Summary |
|----------------|----------|---------|
| `voltnuerongrid-core` | **STUB** | Constant only; no shared types. **Gap:** extract shared error/types from monolith or delete stub policy. |
| `voltnuerongrid-meta` | **STUB** | Constant only. **Gap:** real catalog service or merge into store. |
| `voltnuerongrid-failover` | **STUB** | Constant only; failover logic in daemon. **Gap:** library-ize failover domain. |
| `voltnuerongrid-sql` | **PARTIAL** | Analyzer/classifier, i18n, function registry contracts, legacy aggregation eval, tests. **Gap:** real parser; planner/optimizer; conformance suite. |
| `voltnuerongrid-exec` | **PARTIAL** | `HtapQueryRouter` heuristic routing only. **Gap:** cost model; planner integration; executor integration. |
| `voltnuerongrid-store` | **PARTIAL** | WAL/checkpoint **in-memory** engine, file WAL adapter, indexes, constraints, DDL catalog module, htap sync scaffolding. **Gap:** on-disk row store + MVCC + page cache; columnar; recovery story at scale. |
| `voltnuerongrid-ingest` | **PARTIAL → strong scaffold** | CSV/JSON/Parquet/Excel, registry, event bus/replay, batch/parallel config, chunked loader (per tracker evolution). **Gap:** enterprise connectors; exactly-once to durable tables; schema evolution. |
| `voltnuerongrid-opt` | **PARTIAL** | Distributed cache manager, eviction, circuit breaker patterns. **Gap:** cluster-wide consistency; integration with real commit path. |
| `voltnuerongrid-auth` | **PARTIAL** | Security config, KMS adapters surface, RBAC matrix. **Gap:** full TLS/mTLS server stack; production KMS HSM paths. |
| `voltnuerongrid-audit` | **PARTIAL** | Append-only in-memory sink; small. **Gap:** durable audit store; tamper evidence. |
| `voltnuerongrid-plugins` | **PARTIAL** | Manifest signing, lifecycle, provenance types. **Gap:** sandboxed plugin runtime loading; connector SDK execution. |
| `voltnuerongrid-ai` | **PARTIAL** | Small serde types for autonomous records. **Gap:** model gateway, tool execution boundaries. |
| `voltnuerongrid-driver-rust` | **PARTIAL** | Client config, pool simulation. **Gap:** align with final wire protocol; non-HTTP transport. |
| `voltnuerongridd` | **PARTIAL (broad)** | Integrates almost everything via HTTP. **Gap:** split modules/crates; reduce monolith; implement real data plane. |
| `voltnuerongrid-audit-companion` | **PARTIAL** | CLI merge/report. **Gap:** parity with enterprise audit requirements. |

---

## 3) Cross-cutting gap list (prioritized)

These are **product/engine gaps**, not test gaps.

| ID | Gap | Why it matters | Suggested owner |
|----|-----|----------------|-----------------|
| G-01 | **Monolithic `main.rs`** | Hard to evolve, review, and reuse; hides “services” that design doc shows as separate. | Platform |
| G-02 | **No real distributed control plane** | Design: Raft metadata, schedulers. Code: mostly single-process HTTP. | Distributed |
| G-03 | **OLAP path not a columnar engine** | Routing can label OLAP; execution is not MPP/columnar at scale. | Query |
| G-04 | **OLTP path not durable MVCC row store** | Store crate has primitives; not a full transactional storage engine. | Storage |
| G-05 | **SQL layer is not a full parser/planner** | Analyzer/heuristics + strings; not ANSI conformance driver. | SQL |
| G-06 | **Wire protocol** | Design implies drivers + protocol; primary integration is HTTP JSON in tree. | Integrations |
| G-07 | **Studio UI not in workspace** | Design references `voltnuerongrid-studio` (separate). | UX |
| G-08 | **Connector plugins to real cloud** | Contracts exist; universal secure credential + data movement incomplete. | Ingestion |
| G-09 | **Scale proof** | “Trillion rows” needs benchmarks on real storage + cluster. | Perf |
| G-10 | **Stub crates** | `core`/`meta`/`failover` do not match modular architecture story. | Platform |

---

## 4) Sprint model (2-week sprints, design-aligned)

**Naming:** `Sprint N` = a 2-week increment; dates are **TBD** — adjust to your calendar.  
**Statuses:** `DONE`, `IN_PROGRESS`, `TODO`, `DEFERRED`.

---

### Sprint 1 — Baseline honesty + engineering hygiene

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S1-WS0-01 | WS0 | CI: `cargo check` / `cargo test` stable on Windows/Linux | **DONE** | Per `ci.yml` evolution |
| S1-WS0-02 | WS0 | Gate script conventions + HTTP helper for PS7 | **DONE** | `kpi-http-helpers.ps1`, instructions |
| S1-WS0-03 | WS0 | Document “scaffold vs engine” in tracker | **DONE** | This file + `status_tracker.md` |
| S1-GAP-01 | Platform | Decision: delete vs populate `core`/`meta`/`failover` stubs | **DONE** | G-10: Decision is **keep stubs as-is** (named-constant + `#![forbid(unsafe_code)]`) until a deliberate extraction sprint is planned; policy documented in `copilot-instructions.md` and `status-tracker-v2.md` section 2. No logic may be added to the three stub crates. |

---

### Sprint 2 — Storage engine: from scaffold toward real OLTP

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S2-WS2-01 | WS2 | B-tree index + constraint manager | **DONE** (scaffold) | `voltnuerongrid-store` + HTTP |
| S2-WS2-02 | WS2 | WAL + checkpoint **in-memory** + file adapter patterns | **PARTIAL** | Not full recovery at scale. **Session 13:** WAL forced checkpoint endpoint added — `WalForceCheckpointResponse {status, wal_len_before, wal_len_after, checkpoint_count}`; `POST /api/v1/store/wal/checkpoint` calls `force_checkpoint()` on `InMemoryDurabilityEngine`, returns before/after WAL lengths and total checkpoint count; 2 integration tests `s2_ws2_02_wal_force_checkpoint_*` verify WAL clears to 0 and count increments (including on empty WAL). **Session 9:** WAL COMMIT path now appends every INSERT/UPDATE/DELETE mutation to `InMemoryDurabilityEngine` via `wal.append_mutation()` + `wal.maybe_checkpoint()` (called after each `sql_transaction` COMMIT); `wal_records()` + `checkpoint_count()` accessor methods added to `InMemoryDurabilityEngine` in `crates/voltnuerongrid-store/src/lib.rs`; `wal_engine: Arc<Mutex<InMemoryDurabilityEngine>>` field added to AppState; `GET /api/v1/store/wal/status` + `POST /api/v1/store/wal/recover`; 3 integration tests `s2_ws2_02_*`. **Session 16:** WAL statistics endpoint — `WalStatsResponse {status, record_count, checkpoint_count, mutation_rate_estimate}`; `GET /api/v1/store/wal/stats` reads WAL record count and checkpoint count (no mutation); `mutation_rate_estimate` is a scaffold value equal to `record_count as f64`; requires operator auth; 2 integration tests `s2_ws2_02_wal_stats_*` verify zero counts on fresh state and record count reflected after appends. |
| S2-WS2-03 | WS2 | DDL catalog wired through `sql_execute` | **DONE** (per tracker narrative) | Verify in tree |
| S2-WS2-04 | WS2 | **Page-based row store + MVCC** | **PARTIAL** | **2026-04-12:** `crates/voltnuerongrid-store/src/mvcc.rs` — `PagedRowStore` with `RowVersion`/`MvccRow`/`StorePage` structs; snapshot-read visibility rule (`visible_at(snapshot_xid)`): latest version with `xid ≤ snapshot_xid` that is not a tombstone; `insert()` / `delete()` / `read_at_snapshot()` / `read_latest()` / `scan_at_snapshot()` / `visible_row_count()` API; automatic page split when page reaches `page_size`; `begin_xid()` + `current_xid()` for Xid allocation; 8 unit tests in `mvcc::tests` pass; `PagedRowStore` wired into `AppState.row_store` field (exposed to all handlers). **Session 12:** Snapshot export added — `pub fn export_rows_snapshot(&self) -> Vec<(String, RowData)>` on `PagedRowStore` returns all currently-visible rows at head XID (tombstones excluded); `GET /api/v1/store/rows/snapshot` endpoint requires operator auth, returns `RowSnapshotResponse {status, snapshot_xid, row_count, rows[]}` with per-row `{key, payload}`; 2 integration tests `s2_ws2_04_row_snapshot_*` verify empty-on-fresh-state and row visibility after direct insert. Gap: on-disk page serialisation + recovery. **Session 18:** Row store operational stats — `RowStoreStatsResponse {status, current_xid, total_pages, total_rows, total_visible_rows}`; `GET /api/v1/store/rows/stats` reads `page_count()` / `total_row_count()` / `visible_row_count(current_xid)` from locked `PagedRowStore`; requires operator auth; 2 integration tests `s2_ws2_04_row_store_stats_*` verify zero on fresh state and counts reflect inserts. |
| S2-WS2-05 | WS2 | Integration: transactions commit ↔ store visibility | **PARTIAL** | `AcidTransactionRegistry` tracks `read_snapshot_at_ms` per transaction; `PagedRowStore::read_at_snapshot(snapshot_xid)` implements the visibility protocol; 3 service-level integration tests `s2_ws2_mvcc_*` verify insert/snapshot/delete behaviour through `AppState.row_store`. **2026-04-05:** COMMIT path in `sql_transaction` now calls `extract_insert_row_from_sql()` (uses AST tokenizer) on each statement and flushes extracted rows into `PagedRowStore` with a new `xid`; 1 integration test `s2_ws2_commit_flush_writes_inserts_to_row_store` confirms two-row flush. Remaining TODO: full DML replay (UPDATE/DELETE), conflict-aware xid management. **Session 7:** Write-intent lock table added (`write_intents: HashMap<String,Xid>` on `PagedRowStore`); `begin_write_intent()`/`release_write_intents()`/`was_modified_after()` methods; COMMIT path performs write-write conflict detection — 409 returned when `was_modified_after(key, snapshot_xid_at_begin)` is true; 3 new store unit tests + 2 integration tests `s2_ws2_05_*`. **Session 8:** COMMIT flush path now calls `begin_write_intent(xid, key)` before each `PagedRowStore::insert()` and `release_write_intents(xid)` after all DML writes — completing the write-intent register+release protocol end-to-end. |

---

### Sprint 3 — SQL: parser, planner, conformance

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S3-WS1-01 | WS1 | Statement classifier + `SqlAnalyzer` | **DONE** (partial) | Heuristic/string based |
| S3-WS1-02 | WS1 | `/sql/analyze`, `/sql/route`, `/sql/execute`, `/sql/transaction` + RBAC | **DONE** (scaffold) | `voltnuerongridd` |
| S3-WS1-03 | WS1 | UDF scaffold + contracts in execute path | **DONE** (scaffold) | Not full WASM/JS/Py sandbox in prod sense |
| S3-WS1-04 | WS1 | **Real SQL tokenizer + AST parser (ANSI subset)** | **PARTIAL** | **2026-04-12:** Tokenizer in `crates/voltnuerongrid-sql/src/tokenizer.rs`. **2026-04-05:** Recursive-descent SQL AST parser in `crates/voltnuerongrid-sql/src/ast.rs` — `Statement` enum (Select/Insert/Update/Delete/CreateTable/Begin/Commit/Rollback/Unknown); `SelectStatement` (columns, table, where_clause, group_by, having, order_by, limit); `InsertStatement` (table, columns, multi-row values); `UpdateStatement` (table, assignments, where); `DeleteStatement` (table, where); `CreateTableStatement` (table, column defs); `OrderByClause` (column, descending). `parse_one(sql) -> Result<Statement, String>` entry point; 14 unit tests in `ast::tests` pass. All types re-exported from `voltnuerongrid-sql` crate root. **Session 12:** JOIN clause parsing — `JoinClause { join_table: String, on_condition: Option<String> }` struct added; `join: Option<JoinClause>` field added to `SelectStatement`; `parse_select` now detects `[INNER] JOIN <table> [ON <condition>]` between FROM and WHERE; ON condition parsed up to next keyword; 3 unit tests in new `join_tests` module (`select_with_inner_join_and_on_condition`, `select_with_join_no_on_parses_table`, `select_without_join_leaves_join_none`). SQL crate now **84 tests** (+3). **Session 15:** UNION/set-operation detection — `has_union: bool` field added to `SelectStatement`; `parse_one` detects ` UNION ` in raw SQL (case-insensitive) and sets `has_union = true`; `subquery_tests` module with 3 unit tests (`select_union_sets_has_union_true`, `select_union_all_sets_has_union_true`, `plain_select_has_union_is_false`). SQL crate now **87 tests** (+3). **Session 17:** Subquery / EXISTS detection — `has_subquery: bool` field added to `SelectStatement` (default false); `parse_tokens` SELECT arm detects `(SELECT` (case-insensitive) in raw SQL and sets `has_subquery = true`; `subquery_detection_tests` module with 3 unit tests (`select_with_scalar_subquery_sets_has_subquery_true`, `select_with_in_subquery_sets_has_subquery_true`, `plain_select_has_subquery_is_false`). SQL crate now **90 tests** (+3). Next: planner integration / cost model on top of AST. |
| S3-WS1-05 | WS1 | **Planner/optimizer + cost model** | **PARTIAL** | **Session 13:** `Join` variant added to `LogicalPlan` enum — `Join { left, right, join_table, condition }`; `JoinClause` now exported from `voltnuerongrid-sql` crate root; `plan_select()` checks `sel.join` and wraps Scan in `Join` node when present; `estimate_cost(Join)` returns `Hybrid` path with combined cost + 3.0 penalty; `primary_table()`, `is_read_only()`, `has_aggregation()` all handle `Join`; 2 new unit tests `planner_select_with_join_produces_join_node` + `cost_join_query_is_hybrid_path` — exec crate now **22 tests** (+2). **Session 9:** WHERE predicate pushdown wired into OLAP executor dispatch — `parse_where_predicates(where_clause: &str) -> Option<Vec<VectorizedFilter>>` helper parses space-separated predicates (supports `>=`, `<=`, `!=`, `>`, `<`, `=`); first parseable SELECT in the olap batch has its `where_clause` extracted and pushed into `filter_batch()` before `aggregate_batch()`; integration test `s3_ws1_05_olap_filter_pushdown_reduces_batch` verifies OLAP path returns `olap_agg_results`. **2026-04-05:** `LogicalPlan` enum (Scan/Project/Filter/Aggregate/Sort/Limit/Insert/Update/Delete/CreateTable/Begin/Commit/Rollback/Unknown) + `CostEstimate` + `QueryPlanner` created in `crates/voltnuerongrid-exec/src/planner.rs`; `plan()` converts AST → logical tree (SELECT chain: Scan→Filter→Aggregate→Sort→Limit→Project); `estimate_cost()` produces OLTP/OLAP/Hybrid routing hints; `QueryPath` enum on `AppState` routing call sites. 20 unit tests in exec crate; 2 service-level integration tests `s3_ws1_planner_*` verify OLAP/OLTP routing. **2026-04-05 (session 4):** Planner cost now wired into `sql_route` response — each `RoutedStatementResponse` gains `planner_path`, `estimated_rows`, `relative_cost`; `SqlRouteResponse` gains `batch_estimated_rows` + `batch_relative_cost`; 2 new integration tests `s3_ws1_sql_route_*` verify the aggregate→olap and filter→oltp routing. `sql_execute` response gains `planner_path: Option<String>` (dominant cost path across batch); 1 integration test `s3_ws1_sql_execute_planner_path_populated_for_aggregate` verifies.  **Session 16:** Union/set-operation plan node — `Union { left: Box<LogicalPlan>, right: Box<LogicalPlan> }` variant added to `LogicalPlan` enum; `primary_table()` delegates to left, `has_aggregation()` checks both sides; `estimate_cost(Union)` returns OLAP path with combined cost + 2.0 penalty; `plan_select()` detects `sel.has_union` and wraps projected plan in `Union` node (rhs = synthetic `Scan { table: union_rhs }`); 2 new tests `planner_union_select_produces_union_node` + `cost_union_query_is_olap_path` — exec crate now **24 tests** (+2).|
| S3-WS1-06 | WS1 | **ANSI conformance harness gated in CI** | **PARTIAL** | **2026-04-05 (session 4):** `ansi_conformance` test module added to `crates/voltnuerongrid-sql/src/ast.rs` — 17 test cases covering: SELECT with aliases/DISTINCT, multi-column GROUP BY+HAVING, ORDER BY multi-col, LIMIT, WHERE AND/OR, WHERE numeric, INSERT single/multi-row, UPDATE multi-assign/no-WHERE, DELETE with/without WHERE, CREATE TABLE types, CREATE TABLE IF NOT EXISTS (graceful Unknown), transaction control (BEGIN TRANSACTION / COMMIT WORK / ROLLBACK WORK), unsupported DDL → Unknown (ALTER/DROP/CREATE INDEX/TRUNCATE/GRANT). Gate script `tests/kpi/scripts/run-ws1-ansi-conformance-smoke.ps1` runs `cargo test -p voltnuerongrid-sql ansi_conformance` and emits `tests/kpi/results/ws1/ansi-conformance-smoke.json` (**passed**, 17 conformance tests, 81 total sql-crate tests). **2026-04-05 (session 6):** `run-ws1-gate.ps1` now includes `ws1-ansi-conformance` pack (cargo test `ansi_conformance` filter) as the second pack after `ws1-sql-core-tests` — S3-WS1-06 now gated in the CI gate orchestrator. Next: hook into `run-ws1-gate.ps1`; add SQL-99 JOIN / subquery patterns as parser improves. **Session 19:** 3 new conformance tests: `ansi_select_with_having_clause_parses` (HAVING → group_by populated); `ansi_select_with_limit_and_offset_parses` (LIMIT 25 → `limit=Some(25)`); `ansi_select_with_subquery_sets_has_subquery_flag` (`has_subquery=true`). SQL crate: **93 tests** (+3). |

---

### Sprint 4 — HTAP routing + OLAP execution

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S4-WS3-01 | WS3 | `HtapQueryRouter` + `ws3_*` tests | **DONE** (heuristic) | `voltnuerongrid-exec`; **2026-04-12:** extended with `OVER(` (window functions) + `HAVING` clause detection as OLAP patterns; 18 `ws3_*` tests pass. |
| S4-WS3-02 | WS3 | OLTP vs OLAP **execute** separation in service | **PARTIAL** | Routed; execution simplified. **2026-04-05 (session 4):** `sql_execute` now computes dominant `planner_path` (most expensive statement wins) and returns it in `SqlExecuteResponse.planner_path`; handler uses this alongside `HtapQueryRouter` path to inform future physical executor dispatch. **2026-04-05 (session 5):** Physical OLTP executor dispatch added — when `planner_path == "oltp"`, `sql_execute` calls `execute_oltp_select()` which reads committed rows from `PagedRowStore::scan_at_snapshot()`, applies optional WHERE-clause prefix filter, caps at `max_rows` limit, and returns `oltp_rows: Option<Vec<OltpRowResult>>` in the response; `OltpRowResult { key, data }` carries actual MVCC row data; 2 integration tests `s4_ws3_sql_execute_oltp_*` verify point-read returns real rows and aggregate has no oltp_rows. **Session 8:** OLAP vectorized executor wired — when `planner_path == "olap" | "hybrid"`, `sql_execute` calls `vectorized_scan()` then `aggregate_batch()` with Count op on all columns; results returned as `olap_agg_results: Option<Vec<OlapVecAggResult>>` in `SqlExecuteResponse`; 1 integration test `s4_ws3_02_olap_agg_results_populated_for_aggregate_query` verifies field is populated for OLAP path. Next: predicate pushdown, projection pruning. **Session 17:** Columnar column projection endpoint — `ColumnarProjectQuery {columns: Option<String>}` Query param (`?columns=a,b,c`); `ColumnarProjectResponse {status, rows_scanned, columns_projected, elapsed_us, columns[]}`; `GET /api/v1/store/columnar/project` does full vectorized scan then filters `ColumnBatch` to only columns named in comma-split list (returns all when absent); requires `store` runtime principal; 2 integration tests `s4_ws3_02_columnar_project_*` verify empty-store returns no columns and all-columns returned when no filter specified. |
| S4-WS3-03 | WS3 | **Columnar store + vectorized operators** | **PARTIAL** | **Session 9:** Predicate pushdown operator added — `FilterOp` enum (`Eq/Ne/Gt/Lt/Gte/Lte/Contains`), `VectorizedFilter { column: String, op: FilterOp, value: String }` struct, `pub fn filter_batch(batch: &ColumnBatch, predicates: &[VectorizedFilter]) -> ColumnBatch` function (evaluates each predicate against the Utf8 column, projects passing rows via `project_rows()`); 6 unit tests in `columnar::tests`; store crate now has **70 tests**. Wired into `sql_execute` OLAP path via `parse_where_predicates()`. **2026-04-05 (session 6):** `crates/voltnuerongrid-store/src/columnar.rs` added — `ColumnVector` enum (Int64/Float64/Bool/Utf8/Null), `ColumnBatch` with named typed columns + row_keys, `ColumnBatchBuilder` with automatic type inference (i64→f64→bool→Utf8), `vectorized_scan(rows, limit) -> (ColumnBatch, VectorizedScanStats)`; `GET /api/v1/store/columnar/scan` endpoint materialises committed rows via `PagedRowStore::scan_at_snapshot()` into typed column batches and returns `ColumnarScanResponse {rows_scanned, columns_materialized, elapsed_us, columns[{name,type_hint,row_count,sample_values}]}`; 8 unit tests in `columnar::tests` (row count, type inference, limit, empty); 2 service integration tests `s4_ws3_03_*` verify typed column materialisation and empty-store handling. **Session 7:** Vectorized aggregation added — `VectorizedAggOp{Sum,Count,Avg,Min,Max}`, `AggResult{op,value,row_count}`, `aggregate_column(col, op)->AggResult`, `aggregate_batch(batch, ops)->HashMap<String,AggResult>`; 6 new unit tests in `columnar::tests`. Gap: pushdown predicates; columnar storage layout. **Session 19:** Vectorized aggregate endpoint — `ColumnarAggregateQuery {column: Option<String>, op: Option<String>}`, `ColumnarAggregateResponse {op, column, result, rows_scanned}`; `GET /api/v1/store/columnar/aggregate` runs `aggregate_batch()` via `VectorizedAggOp`; defaults op=count, column=first available; 2 integration tests `s4_ws3_03_columnar_aggregate_*` verify zero-row empty store and count=3 after 3 inserts. |
| S4-WS3-04 | WS3 | **Freshness / sync from OLTP → OLAP** | **PARTIAL** | `htap_sync` scaffold exists. **2026-04-05 (session 5):** COMMIT path in `sql_transaction` now publishes each INSERT/UPDATE/DELETE mutation to `RowStoreSyncOrigin` (table=`"row_store"`, primary_key=extracted key, payload_json=raw SQL); new `POST /api/v1/store/htap/export` endpoint returns pending mutations since a given sequence via `StoreHtapExportResponse { status, since_sequence, mutation_count, checkpoint_last_sequence, mutations[] }`; 2 integration tests `s4_ws3_04_*` verify pending_len grows after COMMIT and export returns mutations with op=`"insert"`. **Session 7:** OLAP consumer side added — `olap_store: Arc<Mutex<HashMap<String,HashMap<String,String>>>>` field in AppState; `POST /api/v1/store/htap/apply` endpoint applies insert/update/delete mutations to olap_store; `GET /api/v1/store/htap/olap/scan` returns all olap_store rows; 2 integration tests `s4_ws3_04_htap_apply_*`. **Session 14:** HTAP lag endpoint added — `HtapLagResponse {status, sync_origin_pending, olap_row_count, estimated_lag_mutations}`; `GET /api/v1/store/htap/lag` reads `sync_origin.pending_len()` vs `olap_store.len()` and computes `estimated_lag_mutations = pending.saturating_sub(olap_rows)`; requires operator store auth; 2 integration tests `s4_ws3_04_htap_lag_*` verify zero-lag on fresh state and row count after apply. |
| S4-WS3-05 | WS3 | Performance gates + trend artifacts | **DONE** (KPI harness) | Not same as prod scale proof |

---

### Sprint 5 — Ingestion + streaming + connectors

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S5-WS4-01 | WS4 | CSV/JSON/Parquet/Excel connectors + HTTP | **DONE** (scaffold) | Base64 for binary |
| S5-WS4-02 | WS4 | Chunked ingest HTTP + async `spawn_blocking` fan-out | **DONE** (scaffold) | Throughput vs real storage still **TODO** (S5-WS4-03) |
| S5-WS4-03 | WS4 | Ingest → **durable typed tables** (not only in-memory maps) | **PARTIAL** | **2026-04-05:** `ingest_csv` and `ingest_json` handlers now write each `IngestRecord` into `PagedRowStore` (field `source: "csv:{connector_id}"` / `"json:{connector_id}"`, field `payload: record.payload`) before the existing in-memory map is updated; 2 integration tests `s5_ws4_row_store_receives_ingest_style_writes` confirm the pattern. **2026-04-05 (continued):** `ingest_parquet` and `ingest_excel` handlers now also write to `PagedRowStore` (same pattern, `source: "parquet:{connector_id}"` / `"excel:{connector_id}"`) — all four ingest formats now write to durable store. **2026-04-05 (session 4):** `POST /api/v1/store/rows/scan` endpoint added — `StoreRowsScanRequest` accepts `snapshot_xid`, `key_prefix`, `limit` (default 1 000, max 10 000); calls `PagedRowStore::scan_at_snapshot()`; requires `store` runtime principal; returns `StoreRowsScanResponse {status, snapshot_xid, row_count, rows}`; 3 new integration tests `s5_ws4_store_rows_scan_*` verify committed-row visibility, prefix filtering, and limit cap. |
| S5-WS4A-01 | WS4A | Outbox + replay + WAL-backed cursors | **DONE** (scaffold) | `voltnuerongrid-ingest` |
| S5-WS4A-02 | WS4A | **Kafka/NATS/Event Hubs live e2e** | **PARTIAL** | **Session 10:** Broker adapter scaffold — `BrokerAdapterInfo {broker_type, enabled, flush_count}`, `BrokerAdapterStatus`, `BrokerFlushRequest`, `BrokerFlushResponse` structs; `broker_flush_counts: Arc<Mutex<HashMap<String,u64>>>` in AppState; `GET /api/v1/ingest/outbox/broker/status` lists kafka/nats/event_hubs adapters with per-broker flush counts; `POST /api/v1/ingest/outbox/broker/flush` dispatches flush (scaffold: derives event count from WAL, increments counter); 2 integration tests `s5_ws4a_02_*`. **Session 14:** Broker health endpoint added — `BrokerHealthEntry {broker_type, flush_count, wal_len, healthy}`, `BrokerHealthResponse {status, broker_count, brokers[]}`; `GET /api/v1/ingest/outbox/broker/health` reports per-broker flush counts vs WAL length (`healthy = flush_count > 0 || wal_len == 0`); 2 integration tests `s5_ws4a_02_broker_health_*` verify 3-broker listing on fresh state and flush_count reflected after insert. Gap: live broker client connections; exactly-once semantics; retry/backoff. |
| S5-E4A-01 | Epic 4A | Connector SDK **runtime load** | **PARTIAL** | **Session 11:** Connector registry scaffold — `ConnectorPlugin {connector_id, connector_type, version, signed, registered_at_ms}` struct; `connector_registry: Arc<Mutex<Vec<ConnectorPlugin>>>` field in AppState; `POST /api/v1/connectors/register` validates presence, rejects duplicates (409), registers and returns `ConnectorRegisterResponse {status, connector_id, registered_at_ms}`; `GET /api/v1/connectors` returns `ConnectorListResponse {status, connector_count, connectors[]}`; both endpoints require admin key; 2 integration tests `s5_e4a_01_*` verify register+list round-trip and duplicate-rejection. Gap: WASM/native library loading; plugin manifest signature verification; hot-reload. **Session 17:** Connector deregister endpoint — `ConnectorDeregisterRequest {connector_id}`, `ConnectorDeregisterResponse {status, connector_id, removed: bool}`; `POST /api/v1/connectors/deregister` removes connector by ID from `connector_registry` vec (retains others); requires operator auth; 2 integration tests `s5_e4a_01_deregister_*` verify known-connector `removed=true` + registry emptied, and unknown-connector `removed=false`. |

---

### Sprint 6 — Security, RBAC, KMS, TLS

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S6-WS5-01 | WS5 | RBAC matrix + runtime principal enforcement | **DONE** (broad) | Across handlers |
| S6-WS5-02 | WS5 | KMS status/outage simulation endpoints | **DONE** (scaffold) | Adapters for local/cloud CLI |
| S6-WS5-03 | WS5 | **TLS/mTLS termination + cert rotation** | **PARTIAL** | **2026-04-05 (session 6):** `GET /api/v1/security/tls/status` endpoint added — reads `SecurityConfigContract.tls_required` + `mtls_required`; reports `cert_source` from `VNG_TLS_CERT_PATH` env var; `cert_rotation_supported: false` (scaffold); requires `security.kms` Read privilege; 1 integration test `s6_ws5_03_tls_status_returns_contract_flags` verifies default dev config (tls=false/mtls=false). `security.kms` privilege scopes expanded to include `security/tls/status` + `security/tde/status` for DBA/Security/SRE. Gap: actual rustls/native-tls axum-server adapter; cert hot-reload via `VNG_TLS_CERT_PATH`/`VNG_TLS_KEY_PATH`. **Session 15:** TLS cert rotation scaffold — `TlsCertRotateRequest {reason: Option<String>}`, `TlsCertRotateResponse {status, rotation_initiated, cert_source, reason}`; `POST /api/v1/security/tls/rotate` requires `security.kms` Manage privilege (scope added to DBA+Security matrix); checks `VNG_TLS_CERT_PATH` env var (`rotation_initiated = cert_source != "not_configured"`); 2 integration tests `s6_ws5_03_tls_rotate_*` verify auth required and not-configured response. **Session 28:** TLS preflight hardening — `/api/v1/security/tls/status`, `/api/v1/security/tls/rotate`, and `/api/v1/security/tls/cert/info` now report cert+key sources (`VNG_TLS_CERT_PATH`, `VNG_TLS_KEY_PATH`) plus file-existence booleans (`cert_present`, `key_present`) and pair readiness (`cert_pair_configured`/`preflight_ok`); rotation now initiates only when both cert+key files exist; tests updated in `s6_ws5_03_tls_*`. |
| S6-WS5-04 | WS5 | **TDE for data at rest in engine** | **PARTIAL** | **2026-04-05 (session 6):** `GET /api/v1/security/tde/status` endpoint added — reads `SecurityConfigContract.encryption_at_rest_required`; reports `tde_active` (true when `encryption_at_rest_required && KMS key env var resolved); reports `key_env_var` from `kms_key_ref_env`; requires `security.kms` Read privilege; 1 integration test `s6_ws5_04_tde_status_reports_encryption_at_rest_required` verifies default config (encryption_at_rest_required=true, tde_active=false in test env). **Session 11:** TDE toggle endpoint — `TdeToggleRequest {enable}`, `TdeToggleResponse {status, tde_active, override_applied}` structs; `tde_override: Arc<Mutex<Option<bool>>>` field in AppState; `POST /api/v1/security/tde/toggle` requires `security.kms` Manage privilege (`security/tde/toggle` scope added to DBA+Security matrix); stores override in AppState; 2 integration tests verify enable/disable round-trip. Gap: actual AES-128-CTR page encryption in PagedRowStore; KMS-backed key wrapping; key rotation hooks. **Session 19:** TDE runtime override status — `TdeOverrideStatusResponse {status, override_set, override_value, effective_tde_active}`; `GET /api/v1/security/tde/override-status` reads `tde_override` mutex, resolves effective state (override takes precedence over config default); requires `security/tde/status` Read privilege; 2 integration tests `s6_ws5_04_tde_override_status_*` verify no-override defaults to config and disable-toggle reflected in effective state. |

---

### Sprint 7 — HA/FT, distributed control plane

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S7-WS6-01 | WS6 | Failover simulation + handoff report + transport abstraction | **DONE** (scaffold) | In daemon |
| S7-WS6-02 | WS6 | **Raft / quorum metadata service** | **PARTIAL** | **Session 13:** Raft log entries endpoint added — `RaftLogResponse {status, log_length, commit_index, entries: Vec<RaftLogEntry>}`; `GET /api/v1/cluster/raft/log` returns current node's full replicated log with commit_index; `RaftLogEntry` (already `Serialize`) exported in raft import; 2 integration tests `s7_ws6_02_raft_log_*` verify empty-log on fresh state and entry presence after append. **Session 7:** `services/voltnuerongridd/src/raft.rs` created with `RaftRole{Follower,Candidate,Leader}`, `RaftLogEntry{index,term,command}`, `RaftVoteRequest/Response`, `RaftAppendRequest/Response`, `RaftStatusSnapshot`, `RaftNode` with `handle_vote_request()`/`handle_append_entries()`/`become_candidate/leader/follower()`/`status()`/`last_log_position()` methods; `raft_state: Arc<Mutex<RaftNode>>` field added to AppState; `GET /api/v1/cluster/raft/status`, `POST /api/v1/cluster/raft/vote`, `POST /api/v1/cluster/raft/append` endpoints registered; 6 unit tests in `raft::tests` + 3 integration tests `s7_ws6_02_*`. Gap: network election timer/heartbeat, actual multi-node coordination. **Session 16:** Raft heartbeat endpoint — `RaftHeartbeatResponse {status, role, term, ticks_reset_to, heartbeat_accepted}`; `POST /api/v1/cluster/raft/heartbeat` resets `ticks_since_heartbeat` to 0 and returns current role + term (simulates receiving a heartbeat from the leader); 2 integration tests `s7_ws6_02_raft_heartbeat_*` verify tick counter reset to 0 and current_term reflected in response. |
| S7-WS6-03 | WS6 | **Automatic leader election + fencing** | **PARTIAL** | **Session 8:** Election timeout scaffold added to `raft.rs` — `ticks_since_heartbeat: u64` and `election_timeout_ticks: u64` fields on `RaftNode` (default timeout: 10); `tick()` method increments counter and calls `become_candidate()` when Follower reaches timeout; `handle_append_entries()` resets tick counter on leader heartbeat; `RaftStatusSnapshot` surfaces tick fields; `POST /api/v1/cluster/raft/tick` endpoint triggers one tick and returns `RaftTickResponse { status, ticks_since_heartbeat, role, current_term, election_triggered }`; 4 new unit tests in `raft::tests` + 2 integration tests `s7_ws6_03_*`. **Session 11:** Fencing token — `fencing_token: u64` field added to both `RaftNode` and `RaftStatusSnapshot`; initialized to 0 in `RaftNode::new()`; incremented in `become_leader()` — every successful leader election bumps the monotonic token; `status()` propagates `fencing_token` to snapshot; `GET /api/v1/cluster/raft/fence` returns `RaftFenceResponse { status, fencing_token, role, current_term }` under admin key guard; 2 integration tests `s7_ws6_03_raft_fence_*` verify token returned and becomes_leader increments. Gap: clock-driven tick loop; token broadcast to followers; lease-based fencing enforcement. **Session 17:** Cluster member list endpoint — `RaftMemberEntry {node_id, role, term, fencing_token}`, `RaftMemberListResponse {status, member_count, members[]}`; `GET /api/v1/cluster/raft/members` returns current node as sole member (scaffold — multi-node membership requires gossip/config protocol); 2 integration tests `s7_ws6_03_raft_member_list_*` verify single-node listing and term reflected in member entry. |
| S7-WS6-04 | WS6 | Chaos/game-day automation vs **real** cluster | **PARTIAL** | **Session 9:** Chaos injection scaffold added — `ChaosEvent {fault_type, target_node, parameters, injected_at_ms, cleared_at_ms}`, `ChaosState {active_faults, event_history}`, `ChaosInjectRequest`, `ChaosStatusResponse` structs; `chaos_state: Arc<Mutex<ChaosState>>` field in AppState; `POST /api/v1/cluster/chaos/inject` pushes a new `ChaosEvent` to active_faults; `POST /api/v1/cluster/chaos/clear` drains active_faults → event_history (with cleared_at_ms timestamp); `GET /api/v1/cluster/chaos/status` returns `{status, active_fault_count, total_injected, active_faults[]}`; 3 integration tests `s7_ws6_04_*` verify empty-on-fresh, inject records fault, clear preserves history. **Session 14:** Chaos health check added — `ChaosHealthResponse {status, cluster_healthy, active_fault_count, history_len}`; `GET /api/v1/cluster/chaos/health` reports `cluster_healthy = active_faults.is_empty()`; 2 integration tests `s7_ws6_04_chaos_health_*` verify healthy on fresh state and unhealthy when fault injected. Gap: real cluster targeting; automated failure injection against running nodes; recovery validation. **Session 18:** Chaos event history endpoint — `ChaosHistoryResponse {status, history_len, events[]}`; `GET /api/v1/cluster/chaos/history` returns the full `event_history` Vec from `ChaosState` (cleared faults only); 2 integration tests `s7_ws6_04_chaos_history_*` verify empty on fresh state and single cleared event appears after inject+clear. |

---

### Sprint 8 — Drivers, Studio, IDE, SaaS deploy

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S8-WS10-01 | WS10 | Rust driver contracts + pool behaviors | **DONE** (scaffold) | HTTP-oriented |
| S8-WS10-02 | WS10 | **Stable wire protocol + multi-language SDKs** | **PARTIAL** | **Session 13:** Driver disconnect endpoint added — `DriverDisconnectRequest {session_token}`, `DriverDisconnectResponse {status, session_token, disconnected: bool}`; `POST /api/v1/driver/disconnect` removes session from `driver_sessions` map; 2 integration tests `s8_ws10_02_driver_disconnect_*` verify remove-existing + missing-returns-false. **Session 10:** Driver wire protocol info + session negotiation scaffold — `DriverProtocolInfo {protocol_version, encoding, auth_modes, supported_statements, max_batch_size}`, `DriverConnectRequest`, `DriverConnectResponse`, `DriverSession {driver_name, driver_version, connected_at_ms}` structs; `driver_sessions: Arc<Mutex<HashMap<String,DriverSession>>>` in AppState; `GET /api/v1/driver/protocol/info` + `POST /api/v1/driver/connect`; 2 integration tests `s8_ws10_02_*`. Gap: binary framing; TLS channel; multi-language SDK codegen. **Session 15:** Driver session list — `DriverSessionInfo {session_token, driver_name, driver_version, connected_at_ms}`, `DriverSessionListResponse {status, session_count, sessions[]}`; `GET /api/v1/driver/sessions` lists all active sessions from `driver_sessions` map; requires operator auth; 2 integration tests `s8_ws10_02_driver_session_list_*` verify empty on fresh state and session appears after insert. |
| S8-WS9-01 | WS9 | Studio API contract scripts | **DONE** (harness) | UI separate repo |
| S8-WS9A-01 | WS9A | IDE adapter manifests + smoke | **DONE** (harness) | |
| S8-REQ08-01 | REQ-08 | Deploy profile smoke (files/Helm) | **DONE** (per tracker) | Live cloud **DEFERRED** (credentials) |

---

### Sprint 9 — Autonomous + AI + audit hardening

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S9-WS8-01 | WS8 | Autonomous records, guardrails, emergency stop | **DONE** (scaffold) | |
| S9-WS8-02 | WS8 | **Production policy + model gateway isolation** | **PARTIAL** | **2026-04-05 (session 6):** `ModelGatewayPolicy { isolation_enabled, allowed_models, max_tokens_per_request, rate_limit_rpm }` added to `AppState` (default: isolation=true, 4096 tokens, 60 rpm); `GET /api/v1/ai/policy` returns current policy (requires `ai.governance` Read); `POST /api/v1/ai/policy/update` updates all fields atomically (requires `ai.governance` Manage); `ai.governance` resource added to privilege matrix for DBA/Security (Manage+Read) and AiOperator (Read); 2 integration tests `s9_ws8_02_*` verify default isolation=true and update persists new values. **Session 8:** Per-model-identity rate limiter added — `ai_request_counters: Arc<Mutex<HashMap<String,u64>>>` field in AppState; `POST /api/v1/ai/request` endpoint increments counter per `model_id`, rejects with 429 when `tokens > max_tokens_per_request`, returns `AiRequestResponse { status, model_id, request_count, rate_limit_rpm, tokens_checked }`; 2 integration tests `s9_ws8_02_ai_request_rate_check_*` verify allow-within-limit and reject-over-token-limit. **Session 12:** Model allowlist enforcement tested — when `policy.allowed_models` is non-empty and `req.model_id` is not listed, `ai_rate_check` returns 403 FORBIDDEN; 2 integration tests `s9_ws8_02_ai_request_allowlist_*` verify rejection of unlisted model and acceptance of listed model. **Session 14:** AI policy stats endpoint added — `ModelRequestStat {model_id, request_count}`, `AiPolicyStatsResponse {status, model_count, total_requests, allowed_models_enforced, per_model: Vec<ModelRequestStat>}`; `GET /api/v1/ai/policy/stats` reads `ai_request_counters`, returns per-model totals and `allowed_models_enforced` flag; requires operator auth; 2 integration tests `s9_ws8_02_ai_policy_stats_*` verify zero-count on fresh state and counts reflected after direct counter insert. Gap: allowlist enforcement on autonomous action endpoints; model isolation beyond the AI request path. **Session 18:** AI policy counter reset — `AiPolicyResetResponse {status, models_cleared}`; `POST /api/v1/ai/policy/reset` locks `ai_request_counters`, records count, calls `clear()`; requires operator auth; 2 integration tests `s9_ws8_02_ai_policy_reset_*` verify `models_cleared=2` when seeded and `0` on empty state. |
| S9-WS8A-01 | WS8A | Audit companion CLI | **PARTIAL** | **Session 19:** Audit CLI summary endpoint — `AuditCliSummaryResponse {status, total_events, chain_valid, last_event_kind, export_hint}`; `GET /api/v1/audit/cli/summary` reads audit sink, calls `verify_chain()`, extracts last event kind; requires `audit.read` privilege; `export_hint` points operators to `/api/v1/audit/export`; 2 integration tests `s9_ws8a_01_audit_cli_summary_*` verify empty-state defaults and 2-event last_event_kind detection. |
| S9-WS8A-02 | WS8A | **Durable tamper-evident audit chain** | **PARTIAL** | **2026-04-05 (session 5):** `AuditEvent` gains `chain_hash: String`; `AppendOnlyAuditSink` gains `prev_chain_hash: String` (seeded with genesis constant) and computes `chain_hash = fnv1a_64(prev_hash|event_id|actor|action|outcome|details_json)` for each appended event; `AppendOnlyAuditSink::verify_chain(events)` static method recomputes chain from genesis and returns `false` on any tamper; `is_empty()` + `all()` accessors added; 5 new tests in `voltnuerongrid-audit` (`chain_hashes_non_empty_deterministic`, `verify_chain_clean`, `verify_chain_tampered`, `verify_chain_empty`); `GET /api/v1/audit/chain/verify` endpoint returns `{ chain_valid: bool, event_count, genesis_hash }` (requires audit Read privilege); 2 service-level integration tests `s9_ws8a_02_*` verify clean chain valid + events have non-empty hashes. **Session 7:** File-backed durability added — `audit_log_path: Option<String>` field in AppState (driven by env var `VNG_AUDIT_LOG_PATH`); `append_audit_event()` and `append_runtime_audit_event()` write JSON-line events to file when path is set; `GET /api/v1/audit/export` returns in-memory+file-backed status with `file_backed` flag and `audit_log_path`; `audit.read` resource scope added to DBA/Security privilege matrix; 1 integration test `s9_ws8a_02_audit_export_returns_buffered_events`. **Session 11:** Cursor-based pagination added to `audit_export` — `AuditExportQuery {cursor: Option<usize>, limit: Option<usize>}` Query params; handler slices the in-memory event buffer using `cursor` offset (default 0) and `limit` (default/max 1000); `AuditExportResponse` extended with `total_event_count: usize`, `cursor: usize`, `limit: usize` fields; 2 integration tests `s9_ws8a_02_audit_export_pagination_*` verify first-page and second-page cursor advancement. **Session 16:** Audit integrity snapshot endpoint — `AuditSnapshotResponse {status, snapshot_at_ms, event_count, chain_valid, genesis_hash}`; `GET /api/v1/audit/snapshot` reads all audit events, calls `AppendOnlyAuditSink::verify_chain()` inline, returns point-in-time count + chain validity + genesis hash; requires `audit.read` privilege; 2 integration tests `s9_ws8a_02_audit_snapshot_*` verify empty chain valid and 2-event chain valid. |

---

### Sprint 10 — Competitive / Epic 15 (optional track)

| ID | Workstream | Task | Status | Notes |
|----|------------|------|--------|--------|
| S10-WS15-01 | WS15 | Competitive matrix + backlog scoring | **DONE** (harness) | `reference/competitive/*` |
| S10-WS15-02 | WS15 | **CDC, follower reads, vector/graph** | **PARTIAL** | **Session 10:** CDC stream scaffold — `CdcEvent {sequence, op, table_name, key, payload, captured_at_ms}`, `CdcStreamResponse` structs; `GET /api/v1/store/cdc/stream` reads WAL engine records and maps each WAL record to a `CdcEvent` (op=`"delete"` when value=`"__deleted__"`, else `"insert"`); 2 integration tests `s10_ws15_02_*` verify empty stream on fresh state and ≥2 events after a 2-INSERT COMMIT. **Session 12:** Per-table CDC cursor tracking — `CdcCursorQuery {table}`, `CdcCursorAdvanceRequest {table_name, position}`, `CdcCursorResponse {status, table_name, cursor_position}` structs; `cdc_cursors: Arc<Mutex<HashMap<String,u64>>>` field in AppState; `GET /api/v1/store/cdc/cursor?table={name}` reads cursor position (defaults to 0 for unknown table); `POST /api/v1/store/cdc/cursor/advance` sets cursor to a given position (requires operator auth); 2 integration tests `s10_ws15_02_cdc_cursor_*` verify fresh-state returns 0 and advance persists position. Gap: update/upsert CDC op detection; per-table stream filtering using cursor; follower reads; vector/graph indexes. **Session 15:** CDC stream table filter — `CdcStreamFilterQuery {table: Option<String>}`, `CdcStreamFilterResponse {status, table_filter, event_count, events[]}`; `GET /api/v1/store/cdc/stream/filter` reads WAL then filters by `table_name` when `?table=X` is set; 2 integration tests `s10_ws15_02_cdc_stream_filter_*` verify matching table returns events and unknown table returns empty. **Session 18:** CDC stream latest N events — `CdcLatestQuery {limit: Option<usize>}`, `CdcLatestResponse {status, event_count, limit_applied, events[]}`; `GET /api/v1/store/cdc/stream/latest` takes tail of WAL events up to `limit` (default 10, max 1000); 2 integration tests `s10_ws15_02_cdc_stream_latest_*` verify empty on fresh state and `limit=3` returns last 3 of 5 events. |

---

## 5) Workstream (WS) rollup — completion snapshot

Use this as an **executive rollup**. “DONE” here means **done for current scaffold scope**, not “done vs full design doc.”

| WS | Done in codebase today | Main remaining gap |
|----|-------------------------|--------------------|
| WS0 | CI, gates, scripts, workspace | Modularize monolith |
| WS1 | Analyzer/execute/transaction/UDF scaffold; AST parser; logical planner wired into sql_route + sql_execute; ANSI conformance now gated in `run-ws1-gate.ps1` (S3-WS1-06) | Physical executor + planner-driven scan/join strategies |
| WS1A | Legacy agg eval + parity scripts (Ready for Validation) | Full MDAP parity operators (P0–P2) |
| WS2 | Indexes, constraints, WAL patterns | Real on-disk row store + MVCC |
| WS2A | Sync-origin scaffold | End-to-end HTAP freshness |
| WS3 | Routing heuristics + `OVER`/`HAVING` detection, perf harness; physical OLTP executor dispatch (point SELECT → PagedRowStore); HTAP sync origin wired to COMMIT path; **columnar batch layout** (`ColumnBatch`/`ColumnVector`/`vectorized_scan`) + `GET /api/v1/store/columnar/scan` | Columnar aggregation operators; vectorized execution engine; OLAP consumer for sync mutations |
| WS4 | Multi-format ingest, chunked (Ready for Validation) | Durable table load |
| WS4A | Outbox/replay (Ready for Validation) | External broker hardening |
| WS5 | RBAC, KMS simulation; **TLS status endpoint** (`GET /api/v1/security/tls/status`); **TDE status endpoint** (`GET /api/v1/security/tde/status`) | Actual rustls/TDE page encryption; cert rotation |
| WS6 | Simulated failover narrative | Real quorum/fencing |
| WS7 | Plugin manifest security | Plugin execution sandbox |
| WS8 | Autonomous API scaffold; **model gateway policy** (`ModelGatewayPolicy` in AppState, `GET/POST /api/v1/ai/policy`) | Actual model-identity enforcement on action endpoints; rate-limiter middleware |
| WS8A | Audit types/companion | Durable audit |
| WS9 | API contract checks | Studio product |
| WS9A | IDE manifests | Extension features |
| WS10 | Rust driver scaffold | Wire protocol |
| WS11 | i18n endpoint/catalog | Collation/UTF-8 depth |
| WS12 | SRE/DR endpoints | Real cluster automation |
| WS13 | Cloud profile files/smoke | Live cloud |
| WS14 | Config schema gates | Central config service |
| WS15 | Matrix/backlog artifacts | Feature implementation |

---

## 6) Is “all development completed?”

**No.**

- **A large amount of *integration and control-plane scaffolding* is implemented in Rust** (especially `voltnuerongridd`) **with strong automated verification** (unit tests + KPI gates).  
- **The core database engine capabilities described in the architecture** (distributed metadata, durable MVCC row store at scale, columnar OLAP execution, universal wire protocol, full SQL conformance, production security termination, trillion-row proof) are **largely not DONE**.

Use **Section 3 (gaps)** + **Sprints 2–4** as the critical path toward a “real database” under the design doc, not more smoke tests alone.
**2026-04-05 progress note:** AST parser added (`crates/voltnuerongrid-sql/src/ast.rs`, 14 unit tests, `parse_one()` entry point, all types re-exported); S5-WS4-03 advanced TODO→PARTIAL (ingest_csv/ingest_json now write to PagedRowStore); S2-WS2-05 further advanced (COMMIT path flushes INSERT rows to PagedRowStore via `extract_insert_row_from_sql()`). Total `cargo test` count: **176 passing in voltnuerongridd** (+6 new integration tests); **48 passing in voltnuerongrid-sql** (+14 AST parser tests).

**2026-04-05 (session 6) progress note:** S4-WS3-03 advanced TODO→PARTIAL (columnar.rs module: `ColumnBatch`/`ColumnVector`/`ColumnBatchBuilder`/`vectorized_scan()`, 8 unit tests; `GET /api/v1/store/columnar/scan` endpoint, 2 service tests `s4_ws3_03_*`); S3-WS1-06 gate hookup complete (`ws1-ansi-conformance` pack added to `run-ws1-gate.ps1`); S6-WS5-03 advanced TODO→PARTIAL (`GET /api/v1/security/tls/status` endpoint + privilege matrix scopes, 1 test); S6-WS5-04 advanced TODO→PARTIAL (`GET /api/v1/security/tde/status` endpoint, 1 test); S9-WS8-02 advanced TODO→PARTIAL (`ModelGatewayPolicy` in AppState, `GET /api/v1/ai/policy` + `POST /api/v1/ai/policy/update` endpoints, `ai.governance` privilege matrix, 2 tests). Total `cargo test`: **198 passing in voltnuerongridd** (+6 new tests), **55 passing in voltnuerongrid-store** (+8 columnar tests), all other crates unchanged.

**2026-04-05 (session 5) progress note:** S4-WS3-02 advanced (physical OLTP executor dispatch: `execute_oltp_select()` reads committed rows from `PagedRowStore`, `oltp_rows` field in `SqlExecuteResponse`); S4-WS3-04 advanced TODO→PARTIAL (COMMIT publishes to `RowStoreSyncOrigin`, `POST /api/v1/store/htap/export` endpoint); S9-WS8A-02 advanced TODO→PARTIAL (FNV-1a chain hashing in `AppendOnlyAuditSink`, `verify_chain()`, `GET /api/v1/audit/chain/verify`). Total `cargo test`: **192 passing in voltnuerongridd** (+6 new tests), **81 passing in voltnuerongrid-sql** (unchanged), **20 passing in voltnuerongrid-exec** (unchanged), **6 passing in voltnuerongrid-audit** (+5 chain tests).

**2026-04-05 (session 4) progress note:** S3-WS1-05 further advanced (planner cost wired into `sql_route` per-statement response + `sql_execute` dominant path; `RoutedStatementResponse` gains `planner_path`/`estimated_rows`/`relative_cost`; `SqlRouteResponse` gains `batch_estimated_rows`/`batch_relative_cost`; `SqlExecuteResponse` gains `planner_path: Option<String>`); S3-WS1-06 advanced TODO→PARTIAL (17 ANSI conformance tests in `ast::ansi_conformance`, gate script `run-ws1-ansi-conformance-smoke.ps1` **passed**, artifact `tests/kpi/results/ws1/ansi-conformance-smoke.json`); S5-WS4-03 advanced (scan endpoint `POST /api/v1/store/rows/scan` added, 3 integration tests); S4-WS3-02 updated (planner_path in execute response). Total `cargo test` count: **186 passing in voltnuerongridd** (+6), **81 passing in voltnuerongrid-sql** (+33 conformance), **20 passing in voltnuerongrid-exec** (unchanged).

**2026-04-05 (session 3) progress note:** S3-WS1-05 advanced TODO→PARTIAL (`LogicalPlan` tree + `CostEstimate` + `QueryPlanner` in `crates/voltnuerongrid-exec/src/planner.rs`, 20 unit tests, OLTP/OLAP routing hints); S5-WS4-03 extended (ingest_parquet + ingest_excel handlers now also write to PagedRowStore — all 4 formats covered); S2-WS2-05 extended (UPDATE/DELETE DML now flushed at COMMIT via `extract_update_row_from_sql()` / `extract_delete_key_from_sql()`). 4 new integration tests in `voltnuerongridd`. Total `cargo test` count: **180 passing in voltnuerongridd** (+4), **20 passing in voltnuerongrid-exec** (+20 new crate).

**2026-04-12 progress note:** Sprint 2 (S2-WS2-04, S2-WS2-05) and Sprint 3 (S3-WS1-04) have advanced from TODO → PARTIAL with the addition of `PagedRowStore` (MVCC) and the SQL `Tokenizer` modules. Sprint 1 gap S1-GAP-01 (stub-crate decision) is now DONE. Total `cargo test` count: **170 passing** across all crates.

**2026-04-05 (session 7) progress note:** S2-WS2-05 extended (write-intent lock table: `PagedRowStore.write_intents`, `begin_write_intent()`/`release_write_intents()`/`was_modified_after()` methods; COMMIT path in `sql_transaction` now performs write-write conflict detection — returns 409 on conflict); S4-WS3-03 extended (vectorized aggregation: `VectorizedAggOp{Sum,Count,Avg,Min,Max}`, `AggResult`, `aggregate_column()`, `aggregate_batch()` added to `crates/voltnuerongrid-store/src/columnar.rs`); S4-WS3-04 extended (`POST /api/v1/store/htap/apply` + `GET /api/v1/store/htap/olap/scan` endpoints; `olap_store: Arc<Mutex<HashMap>>` field in AppState); S9-WS8A-02 extended (file-backed audit log: `audit_log_path` field in AppState driven by `VNG_AUDIT_LOG_PATH`, `append_audit_event()` writes JSON-lines; `GET /api/v1/audit/export` endpoint); S7-WS6-02 advanced TODO→PARTIAL (Raft consensus scaffold: `services/voltnuerongridd/src/raft.rs` with `RaftNode`/`RaftRole`/`RaftLogEntry`/vote+append+status RPCs; `GET /api/v1/cluster/raft/status`, `POST /api/v1/cluster/raft/vote`, `POST /api/v1/cluster/raft/append` endpoints; `raft_state: Arc<Mutex<RaftNode>>` in AppState). Store crate: **64 passing** (+9 new: 3 write-intent, 6 aggregation). Service: **212 tests total** (+14 new integration tests).

**2026-04-05 (session 8) progress note:** S2-WS2-05 extended (COMMIT path now calls `begin_write_intent(xid,key)` before each store write + `release_write_intents(xid)` after all DML — completing write-intent register+release protocol); S4-WS3-02 advanced (OLAP vectorized executor wired: `sql_execute` dispatches to `vectorized_scan()`+`aggregate_batch()` for olap/hybrid planner paths; `olap_agg_results` field in `SqlExecuteResponse`); S7-WS6-03 advanced TODO→PARTIAL (Raft election timeout: `ticks_since_heartbeat`/`election_timeout_ticks` fields, `tick()` method, heartbeat reset, `POST /api/v1/cluster/raft/tick` endpoint, 4 new raft unit tests); S9-WS8-02 extended (per-model rate limiter: `ai_request_counters` in AppState, `POST /api/v1/ai/request` endpoint with token budget check + 429 rejection). Service: **221 tests total** (+9 new tests).

**2026-04-06 (session 9) progress note:** S4-WS3-03 extended PARTIAL (predicate pushdown: `FilterOp` enum + `VectorizedFilter` struct + `filter_batch()` function in `columnar.rs`; 6 new unit tests; store crate **70 tests**); S7-WS6-04 advanced TODO→PARTIAL (chaos inject/clear/status scaffold: `ChaosEvent`/`ChaosState`/`ChaosInjectRequest`/`ChaosStatusResponse`; `chaos_state` in AppState; 3 endpoints; 3 integration tests `s7_ws6_04_*`); S2-WS2-02 extended PARTIAL (WAL COMMIT path writes: `wal.append_mutation()` after each INSERT/UPDATE/DELETE at COMMIT; `wal_records()`/`checkpoint_count()` accessors in `InMemoryDurabilityEngine`; `wal_engine` in AppState; `GET /api/v1/store/wal/status` + `POST /api/v1/store/wal/recover` endpoints; 3 integration tests `s2_ws2_02_*`); S3-WS1-05 extended PARTIAL (WHERE predicate pushdown in OLAP execute: `parse_where_predicates()` helper; first SELECT where_clause pushed into `filter_batch()` before aggregation; 1 integration test `s3_ws1_05_olap_filter_pushdown_reduces_batch`). Service: **228 tests total** (+7 new integration tests). Store crate: **70 tests** (+6 filter_batch tests).

**2026-04-06 (session 10) progress note:** S8-WS10-02 advanced TODO→PARTIAL (driver protocol info + session connect: `DriverProtocolInfo`/`DriverConnectRequest`/`DriverConnectResponse`/`DriverSession`; `driver_sessions` in AppState; `GET /api/v1/driver/protocol/info` + `POST /api/v1/driver/connect`; 2 integration tests `s8_ws10_02_*`); S10-WS15-02 advanced TODO→PARTIAL (CDC stream: `CdcEvent`/`CdcStreamResponse`; `GET /api/v1/store/cdc/stream` from WAL; 2 integration tests `s10_ws15_02_*`); S5-WS4A-02 advanced TODO→PARTIAL (broker adapter scaffold: `BrokerAdapterInfo`/`BrokerAdapterStatus`/`BrokerFlushRequest`/`BrokerFlushResponse`; `broker_flush_counts` in AppState; `GET /api/v1/ingest/outbox/broker/status` + `POST /api/v1/ingest/outbox/broker/flush`; 2 integration tests `s5_ws4a_02_*`); S9-WS8-02 extended (sliding-window rate limiter: `ai_rate_window_starts` in AppState; `ai_rate_check` now resets counter when 60s window elapses; 1 integration test `s9_ws8_02_rate_window_counter_increments_within_window`). Service: **235 tests total** (+7 new integration tests).

**2026-04-07 (session 11) progress note:** S5-E4A-01 advanced TODO→PARTIAL (connector registry scaffold: `ConnectorPlugin`/`ConnectorRegisterRequest`/`ConnectorRegisterResponse`/`ConnectorListResponse`; `connector_registry` in AppState; `POST /api/v1/connectors/register` + `GET /api/v1/connectors`; 2 integration tests `s5_e4a_01_*`); S7-WS6-03 extended PARTIAL (fencing token: `fencing_token: u64` added to `RaftNode`+`RaftStatusSnapshot`; incremented on `become_leader()`; `GET /api/v1/cluster/raft/fence` returns `RaftFenceResponse`; 2 integration tests `s7_ws6_03_raft_fence_*`); S9-WS8A-02 extended PARTIAL (audit export pagination: `AuditExportQuery {cursor, limit}` Query params; `audit_export` slices events by cursor+limit; `total_event_count`/`cursor`/`limit` added to `AuditExportResponse`; 2 integration tests `s9_ws8a_02_audit_export_pagination_*`); S6-WS5-04 extended PARTIAL (TDE toggle: `TdeToggleRequest`/`TdeToggleResponse`; `tde_override` in AppState; `POST /api/v1/security/tde/toggle`; `security/tde/toggle` scope added to DBA+Security privilege matrix; 2 integration tests). Service: **243 tests total** (+8 new integration tests).

**2026-04-13 (session 15) progress note:** S3-WS1-04 extended PARTIAL (UNION/set-op detection: `has_union: bool` on `SelectStatement`; UNION detected in `parse_one` SELECT arm; 3 tests in `subquery_tests` module — sql crate now **87 tests** (+3)); S6-WS5-03 extended PARTIAL (TLS rotation scaffold: `POST /api/v1/security/tls/rotate`; `TlsCertRotateRequest/Response`; `security/tls/rotate` scope added to DBA+Security privilege matrix; 2 integration tests `s6_ws5_03_tls_rotate_*`); S8-WS10-02 extended PARTIAL (driver session list: `GET /api/v1/driver/sessions`; `DriverSessionInfo/DriverSessionListResponse`; requires operator auth; 2 integration tests `s8_ws10_02_driver_session_list_*`); S10-WS15-02 extended PARTIAL (CDC stream filter: `GET /api/v1/store/cdc/stream/filter`; `CdcStreamFilterQuery/CdcStreamFilterResponse`; 2 integration tests `s10_ws15_02_cdc_stream_filter_*`). SQL crate: **87 tests** (+3). Service: **269 tests total** (+6 new integration tests).

**2026-04-13 (session 16) progress note:** S3-WS1-05 extended PARTIAL (Union plan node: `Union {left, right}` variant in `LogicalPlan`; `plan_select` wraps projected plan in `Union` when `sel.has_union`; `estimate_cost(Union)` → OLAP + 2.0 penalty; 2 tests `planner_union_*` — exec crate **24 tests** +2); S7-WS6-02 extended PARTIAL (Raft heartbeat: `POST /api/v1/cluster/raft/heartbeat` resets `ticks_since_heartbeat` to 0; `RaftHeartbeatResponse`; 2 tests `s7_ws6_02_raft_heartbeat_*`); S2-WS2-02 extended PARTIAL (WAL stats: `GET /api/v1/store/wal/stats`; `WalStatsResponse {record_count, checkpoint_count, mutation_rate_estimate}`; 2 tests `s2_ws2_02_wal_stats_*`); S9-WS8A-02 extended PARTIAL (audit integrity snapshot: `GET /api/v1/audit/snapshot`; `AuditSnapshotResponse {snapshot_at_ms, event_count, chain_valid, genesis_hash}`; 2 tests `s9_ws8a_02_audit_snapshot_*`). Exec crate: **24 tests** (+2). Service: **275 tests total** (+6 new integration tests).

**2026-04-14 (session 19) progress note:** S3-WS1-06 extended PARTIAL (3 ANSI conformance tests: HAVING clause → `group_by` populated; LIMIT+OFFSET → `limit=Some(25)`; subquery → `has_subquery=true` — sql crate **93 tests** +3); S4-WS3-03 extended PARTIAL (columnar aggregate: `GET /api/v1/store/columnar/aggregate`; `ColumnarAggregateQuery/Response`; 2 tests `s4_ws3_03_columnar_aggregate_*`); S6-WS5-04 extended PARTIAL (TDE override-status: `GET /api/v1/security/tde/override-status`; `TdeOverrideStatusResponse`; 2 tests `s6_ws5_04_tde_override_status_*`); S9-WS8A-01 extended PARTIAL (audit CLI summary: `GET /api/v1/audit/cli/summary`; `AuditCliSummaryResponse`; 2 tests `s9_ws8a_01_audit_cli_summary_*`). SQL crate: **93 tests** (+3). Service: **295 tests total** (+6 new integration tests).

**2026-04-14 (session 18) progress note:** S2-WS2-04 extended PARTIAL (row store stats: `GET /api/v1/store/rows/stats`; `RowStoreStatsResponse {status, current_xid, total_pages, total_rows, total_visible_rows}`; 2 tests `s2_ws2_04_row_store_stats_*`); S7-WS6-04 extended PARTIAL (chaos history: `GET /api/v1/cluster/chaos/history`; `ChaosHistoryResponse {status, history_len, events[]}`; 2 tests `s7_ws6_04_chaos_history_*`); S9-WS8-02 extended PARTIAL (AI policy reset: `POST /api/v1/ai/policy/reset`; `AiPolicyResetResponse {status, models_cleared}`; 2 tests `s9_ws8_02_ai_policy_reset_*`); S10-WS15-02 extended PARTIAL (CDC stream latest: `GET /api/v1/store/cdc/stream/latest`; `CdcLatestQuery {limit}`, `CdcLatestResponse`; 2 tests `s10_ws15_02_cdc_stream_latest_*`). Service: **289 tests total** (+8 new integration tests).

**2026-04-13 (session 17) progress note:** S3-WS1-04 extended PARTIAL (subquery detection: `has_subquery: bool` on `SelectStatement`; `(SELECT` detected in `parse_tokens` SELECT arm; 3 tests in `subquery_detection_tests` module — sql crate **90 tests** +3); S4-WS3-02 extended PARTIAL (columnar projection: `GET /api/v1/store/columnar/project`; `ColumnarProjectQuery/Response`; comma-split `?columns` filter; 2 tests `s4_ws3_02_columnar_project_*`); S5-E4A-01 extended PARTIAL (connector deregister: `POST /api/v1/connectors/deregister`; `ConnectorDeregisterRequest/Response`; removes by ID; 2 tests `s5_e4a_01_deregister_*`); S7-WS6-03 extended PARTIAL (member list: `GET /api/v1/cluster/raft/members`; `RaftMemberEntry/RaftMemberListResponse`; returns local node; 2 tests `s7_ws6_03_raft_member_list_*`). SQL crate: **90 tests** (+3). Service: **281 tests total** (+6 new integration tests).

**2026-04-13 (session 16) progress note:** S3-WS1-05 extended PARTIAL (Union plan node: `Union {left, right}` variant in `LogicalPlan`; `plan_select` wraps projected plan in `Union` when `sel.has_union`; `estimate_cost(Union)` → OLAP + 2.0 penalty; 2 tests `planner_union_*` — exec crate **24 tests** +2); S7-WS6-02 extended PARTIAL (Raft heartbeat: `POST /api/v1/cluster/raft/heartbeat` resets `ticks_since_heartbeat` to 0; `RaftHeartbeatResponse`; 2 tests `s7_ws6_02_raft_heartbeat_*`); S2-WS2-02 extended PARTIAL (WAL stats: `GET /api/v1/store/wal/stats`; `WalStatsResponse {record_count, checkpoint_count, mutation_rate_estimate}`; 2 tests `s2_ws2_02_wal_stats_*`); S9-WS8A-02 extended PARTIAL (audit integrity snapshot: `GET /api/v1/audit/snapshot`; `AuditSnapshotResponse {snapshot_at_ms, event_count, chain_valid, genesis_hash}`; 2 tests `s9_ws8a_02_audit_snapshot_*`). Exec crate: **24 tests** (+2). Service: **275 tests total** (+6 new integration tests).

**2026-04-06 (session 12) progress note:** S3-WS1-04 extended PARTIAL (JOIN clause parsing: `JoinClause {join_table, on_condition}` struct; `join: Option<JoinClause>` added to `SelectStatement`; `parse_select` detects `JOIN <table> [ON <cond>]`; 3 unit tests in `join_tests` module — SQL crate now **84 tests** (+3)); S2-WS2-04 extended PARTIAL (snapshot export: `export_rows_snapshot()` on `PagedRowStore`; `GET /api/v1/store/rows/snapshot` returns `RowSnapshotResponse`; 2 integration tests `s2_ws2_04_row_snapshot_*`); S9-WS8-02 extended PARTIAL (model allowlist: `ai_rate_check` already enforces `allowed_models` list; 2 integration tests `s9_ws8_02_ai_request_allowlist_*` verify reject-unlisted + permit-listed); S10-WS15-02 extended PARTIAL (per-table CDC cursors: `cdc_cursors` in AppState; `GET /api/v1/store/cdc/cursor` + `POST /api/v1/store/cdc/cursor/advance`; 2 integration tests `s10_ws15_02_cdc_cursor_*`). Service: **249 tests total** (+6 new integration tests).

**2026-04-13 (session 13) progress note:** S3-WS1-05 extended PARTIAL (JOIN planner: `Join {left,right,join_table,condition}` variant added to `LogicalPlan`; `JoinClause` exported from `voltnuerongrid-sql` crate root; `plan_select()` wraps Scan in `Join` node when `sel.join` present; `estimate_cost(Join)` → `Hybrid` + 3.0 penalty; `primary_table()` / `is_read_only()` / `has_aggregation()` handle `Join`; 2 new tests `planner_select_with_join_produces_join_node` + `cost_join_query_is_hybrid_path` — exec crate **22 tests** (+2)); S8-WS10-02 extended PARTIAL (driver disconnect: `DriverDisconnectRequest`/`DriverDisconnectResponse`; `POST /api/v1/driver/disconnect` removes session from `driver_sessions`; 2 integration tests `s8_ws10_02_driver_disconnect_*`); S7-WS6-02 extended PARTIAL (raft log endpoint: `RaftLogResponse {status, log_length, commit_index, entries}`; `GET /api/v1/cluster/raft/log` returns node's replicated log; 2 integration tests `s7_ws6_02_raft_log_*`); S2-WS2-02 extended PARTIAL (WAL checkpoint: `WalForceCheckpointResponse`; `POST /api/v1/store/wal/checkpoint` calls `force_checkpoint()`; 2 integration tests `s2_ws2_02_wal_force_checkpoint_*`). Exec crate: **22 tests** (+2). Service: **255 tests total** (+6 new integration tests).

**2026-04-13 (session 14) progress note:** S7-WS6-04 extended PARTIAL (chaos health check: `ChaosHealthResponse {status, cluster_healthy, active_fault_count, history_len}`; `GET /api/v1/cluster/chaos/health`; `cluster_healthy = active_faults.is_empty()`; 2 integration tests `s7_ws6_04_chaos_health_*`); S4-WS3-04 extended PARTIAL (HTAP lag: `HtapLagResponse {status, sync_origin_pending, olap_row_count, estimated_lag_mutations}`; `GET /api/v1/store/htap/lag` computes `estimated_lag_mutations = pending.saturating_sub(olap_rows)`; 2 integration tests `s4_ws3_04_htap_lag_*`); S5-WS4A-02 extended PARTIAL (broker health: `BrokerHealthEntry {broker_type, flush_count, wal_len, healthy}`; `GET /api/v1/ingest/outbox/broker/health` lists all 3 brokers; 2 integration tests `s5_ws4a_02_broker_health_*`); S9-WS8-02 extended PARTIAL (AI policy stats: `ModelRequestStat`, `AiPolicyStatsResponse {status, model_count, total_requests, allowed_models_enforced, per_model[]}`; `GET /api/v1/ai/policy/stats`; 2 integration tests `s9_ws8_02_ai_policy_stats_*`). Service: **263 tests total** (+8 new integration tests).

**2026-04-14 (session 20) progress note:** S5-WS4-03 extended PARTIAL (ingest schema registry: `GET /api/v1/ingest/schema`; `IngestSchemaColumn`, `IngestSchemaEntry`, `IngestSchemaRegistryResponse`; enumerates csv+json connector maps, infers columns; 2 tests `s5_ws4_03_ingest_schema_*`); S2-WS2-05 extended PARTIAL (transaction isolation stats: `GET /api/v1/sql/transactions/isolation`; `TxIsolationEntry {transaction_id,isolation_level,snapshot_xid,statement_count}`, `TxIsolationStatsResponse`; 2 tests `s2_ws2_05_isolation_stats_*`); S7-WS6-02 extended PARTIAL (raft commit progress: `GET /api/v1/cluster/raft/commit`; `RaftCommitProgressResponse {commit_index,last_applied,log_length,uncommitted}`; 2 tests `s7_ws6_02_raft_commit_progress_*`); S8-WS10-02 extended PARTIAL (driver health: `GET /api/v1/driver/health`; `DriverHealthResponse {active_sessions,pool_circuit_breaker,pool_active_connections,pool_total_acquired,healthy}`; operator auth; 2 tests `s8_ws10_02_driver_health_*`). Service: **303 tests total** (+8 new integration tests).

**2026-04-06 (session 21) progress note:** S3-WS1-04/S3-WS1-06 extended PARTIAL (window function detection: `has_window_fn: bool` added to `SelectStatement`; `OVER (` / `OVER(` detection in `parse_tokens()`; 3 tests in new `window_fn_tests` module — sql crate **96 tests** +3); S4-WS3-04 extended PARTIAL (HTAP force-sync: `POST /api/v1/store/htap/sync` drains `sync_origin.export_batch()` into `olap_store` then `ack_through(last_seq)`; `HtapForceSyncResponse {mutations_applied, olap_row_count_after}`; 2 tests `s4_ws3_04_htap_force_sync_*`); S7-WS6-02 extended PARTIAL (Raft point-in-time snapshot: `GET /api/v1/cluster/raft/snapshot`; `RaftSnapshotResponse {node_id,term,commit_index,last_applied,log_length,fencing_token}`; 2 tests `s7_ws6_02_raft_snapshot_*`); S2-WS2-04 extended PARTIAL (row store prefix count: `GET /api/v1/store/rows/count?key_prefix=X`; `RowCountQuery`, `RowCountResponse {snapshot_xid,key_prefix,count}`; 2 tests `s2_ws2_04_row_count_*`). SQL crate: **96 tests** (+3). Service: **309 tests total** (+6 new integration tests).

**2026-04-06 (session 22) progress note:** S3-WS1-04 extended PARTIAL (aggregate function detection: `has_agg_fn: bool` added to `SelectStatement`; `COUNT(`, `SUM(`, `AVG(`, `MIN(`, `MAX(` detection in `parse_tokens()` SELECT arm via whitespace-stripped token; 3 tests in new `agg_fn_tests` module — sql crate **99 tests** +3); S5-E4A-01 extended PARTIAL (connector get-by-id: `GET /api/v1/connectors/get?id=X`; `ConnectorGetQuery {id}`, `ConnectorGetResponse {status, found, connector: Option<ConnectorPlugin>}`; 2 tests `s5_e4a_01_connector_get_*`); S9-WS8A-02 extended PARTIAL (audit purge: `POST /api/v1/audit/purge`; `AuditPurgeRequest {confirm}`, `AuditPurgeResponse {status, events_purged, chain_reset}`; replaces in-memory sink with fresh `AppendOnlyAuditSink`; 2 tests `s9_ws8a_02_audit_purge_*`); S10-WS15-02 extended PARTIAL (CDC cursor rewind: `POST /api/v1/store/cdc/cursor/rewind`; `CdcCursorRewindRequest {table_name}`; resets table cursor to 0 in `cdc_cursors`; 2 tests `s10_ws15_02_cdc_cursor_rewind_*`). SQL crate: **99 tests** (+3). Service: **315 tests total** (+6 new integration tests).

**2026-04-06 (session 23) progress note:** S3-WS1-05 extended PARTIAL (WindowFn plan node: `WindowFn { input: Box<LogicalPlan>, window_func: String }` variant added to `LogicalPlan`; `plan_select()` wraps outermost node in `WindowFn` when `sel.has_window_fn`; `estimate_cost(WindowFn)` → OLAP path + 2.5 penalty; `primary_table()` and `has_aggregation()` handle `WindowFn`; 2 tests `planner_window_fn_produces_window_fn_node` + `cost_window_fn_query_is_olap_path` — exec crate **26 tests** +2); S2-WS2-02 extended PARTIAL (WAL replay: `GET /api/v1/store/wal/replay`; `WalReplayQuery {table_filter, op_filter}`, `WalReplayEntry {sequence, key, value}`, `WalReplayResponse {total_records, matched_records, entries}`; filters by key substring and op type (insert/delete); 2 tests `s2_ws2_02_wal_replay_*`); S6-WS5-03 extended PARTIAL (TLS cert info: `GET /api/v1/security/tls/cert/info`; `TlsCertInfoResponse {cert_source, tls_required, mtls_required, cert_rotation_supported}`; 2 tests `s6_ws5_03_tls_cert_info_*`); S8-WS10-02 extended PARTIAL (driver query: `POST /api/v1/driver/query`; `DriverQueryRequest {session_token, sql}`, `DriverQueryResponse {session_token, sql, rows_returned, executed_at_ms}`; validates session token exists — 401 if not; 2 tests `s8_ws10_02_driver_query_*`). Exec crate: **26 tests** (+2). Service: **321 tests total** (+6 new integration tests).

**2026-04-06 (session 24) progress note:** S3-WS1-06 extended PARTIAL (3 more ANSI conformance tests in new `extended_conformance_tests` module: `select_order_by_with_limit_parses_both` (ORDER BY + LIMIT 50), `insert_multi_row_parses_correct_column_count` (2 rows `["ts","msg","LEVEL"]`), `create_table_with_five_columns_parses_all_columns` (5-col CREATE TABLE) — sql crate **102 tests** +3); S7-WS6-03 extended PARTIAL (Raft current leader endpoint: `GET /api/v1/cluster/raft/leader`; `RaftLeaderResponse {node_id, role, current_term, is_leader, fencing_token}`; 2 tests `s7_ws6_03_raft_leader_fresh_state_is_follower` + `s7_ws6_03_raft_leader_reflects_term_after_vote`); S7-WS6-04 extended PARTIAL (Chaos fire drill: `POST /api/v1/cluster/chaos/fire-drill`; `ChaosFireDrillRequest {drill_type, target_node}`, `ChaosFireDrillResponse {status, drill_type, faults_injected, target_node}`; fire_drill pushes to `event_history` and leaves `active_faults` empty; 2 tests `s7_ws6_04_chaos_fire_drill_adds_to_history` + `s7_ws6_04_chaos_fire_drill_with_target_node`); S4-WS3-04 extended PARTIAL (HTAP detailed status: `GET /api/v1/store/htap/status`; `HtapStatusResponse {sync_origin_pending, olap_row_count, last_sync_ms, sync_lag_estimate, is_synchronized}`; uses `pending_len()` from `RowStoreSyncOrigin`; 2 tests `s4_ws3_04_htap_status_empty_state_is_synchronized` + `s4_ws3_04_htap_status_reflects_olap_row_count`). SQL crate: **102 tests** (+3). Service: **327 tests total** (+6 new integration tests).

**2026-04-06 (session 25) progress note:** S3-WS1-04 extended PARTIAL (`is_distinct: bool` added to `SelectStatement`; `SELECT DISTINCT` detection via `up_trim.starts_with("SELECTDISTINCT")` in `parse_tokens()` SELECT arm; 3 tests in new `select_distinct_tests` module: `select_distinct_sets_is_distinct_true`, `select_without_distinct_has_is_distinct_false`, `select_distinct_with_where_sets_flag_and_table` — sql crate **105 tests** +3); S2-WS2-02 extended PARTIAL (WAL compact endpoint: `POST /api/v1/store/wal/compact`; `WalCompactResponse {status, records_before, records_after, checkpoint_count, compacted: bool}`; handler force-checkpoints WAL and returns before/after WAL record counts; `compacted = records_before > records_after`; 2 tests `s2_ws2_02_wal_compact_empty_wal_returns_compacted_false` + `s2_ws2_02_wal_compact_after_mutations_clears_wal`); S7-WS6-02 extended PARTIAL (Raft election timer status: `GET /api/v1/cluster/raft/election/status`; `RaftElectionStatusResponse {status, role, ticks_since_heartbeat, election_timeout_ticks, remaining_ticks, is_election_pending}`; `remaining_ticks = timeout.saturating_sub(ticks)`; `is_election_pending = role == Candidate`; 2 tests `s7_ws6_02_raft_election_status_fresh_state_is_follower` + `s7_ws6_02_raft_election_status_remaining_ticks_decrements`); S9-WS8-02 extended PARTIAL (AI governance audit: `GET /api/v1/ai/governance/audit`; `AiGovernanceAuditEntry {model_id, request_count}`, `AiGovernanceAuditResponse {status, total_models, total_requests, entries[]}`; reads `ai_request_counters`, sorts entries by `request_count` desc; requires operator auth; 2 tests `s9_ws8_02_ai_governance_audit_empty_state_returns_zero` + `s9_ws8_02_ai_governance_audit_reflects_request_counts`). SQL crate: **105 tests** (+3). Service: **333 tests total** (+6 new integration tests).

**2026-04-06 (session 26) progress note:** S3-WS1-04 extended PARTIAL (`offset: Option<u64>` added to `SelectStatement`; OFFSET parsed after LIMIT in `parse_select()` via `find_keyword_from` token-index lookahead — `lp+2` = OFFSET keyword, `lp+3` = number; 3 tests in new `offset_tests` module: `select_with_limit_and_offset_parses_both` (LIMIT 10 OFFSET 5), `select_with_offset_zero_is_stored` (OFFSET 0), `select_without_offset_is_none` — sql crate **108 tests** +3); S3-WS1-05 extended PARTIAL (`Distinct { input: Box<LogicalPlan> }` variant added to `LogicalPlan` enum; `plan_select()` changed to capture `after_window` result and wrap in `Distinct` when `sel.is_distinct`; `estimate_cost(Distinct)` → `estimated_rows = inner/2`, `relative_cost = inner + 0.3`, OLTP path; `primary_table()` and `has_aggregation()` delegate to `input`; 2 tests `planner_distinct_wraps_outermost_in_distinct_node` + `cost_distinct_query_routes_to_oltp` — exec crate **28 tests** +2); S2-WS2-02 extended PARTIAL (WAL bounds endpoint: `GET /api/v1/store/wal/bounds`; `WalBoundsResponse {status, record_count, checkpoint_count, oldest_sequence: Option<u64>, newest_sequence: Option<u64>}`; reads `wal_records().first()` and `.last()` for sequence bounds; 2 tests `s2_ws2_02_wal_bounds_empty_state_shows_none_sequences` + `s2_ws2_02_wal_bounds_after_mutations_shows_sequences`); S10-WS15-02 extended PARTIAL (CDC aggregate metrics: `GET /api/v1/store/cdc/metrics`; `CdcMetricsResponse {status, total_events, insert_count, delete_count, tables_seen}`; counts inserts/deletes via `r.value == "__deleted__"`, extracts table prefix via `r.key.split(':').next()`; 2 tests `s10_ws15_02_cdc_metrics_empty_state_returns_zero_counts` + `s10_ws15_02_cdc_metrics_after_mutations_counts_inserts`). SQL crate: **108 tests** (+3). Exec crate: **28 tests** (+2). Service: **337 tests total** (+4 new integration tests).

**2026-04-07 (session 27) progress note:** S3-WS1-06 new PARTIAL (`has_null_literal: bool` added to `SelectStatement`; detection via `up.contains("IS NULL") || up.contains("IS NOT NULL")` in `parse_tokens()` SELECT arm; 3 tests in new `null_literal_tests` module: `where_is_null_sets_has_null_literal_true`, `where_is_not_null_sets_has_null_literal_true`, `plain_select_has_null_literal_false` — sql crate **111 tests** +3); S3-WS1-06 exec extended (`Offset { input: Box<LogicalPlan>, offset: u64 }` variant added after `Distinct`; `plan_select()` inserts `after_offset` stage between `after_limit` and `after_project` — wraps in `Offset` when `sel.offset > 0`; `estimate_cost(Offset)` → `estimated_rows = inner`, `relative_cost = inner + 0.1`, OLTP; `primary_table()` and `has_aggregation()` delegate to `input`; 2 tests `planner_select_with_offset_produces_offset_node` + `cost_offset_query_routes_to_oltp` — exec crate **30 tests** +2); S2-WS2-02 extended PARTIAL (WAL tail endpoint: `GET /api/v1/store/wal/tail`; `WalTailQuery { limit: Option<usize> }`, `WalTailResponse {status, record_count, limit_applied, entries]}`; handler defaults `limit=10`, clamps `1–1000`, slices last N records from WAL via `skip = total - limit`; operator auth via `require_operator_auth`; 2 tests `s2_ws2_02_wal_tail_empty_returns_zero_entries` + `s2_ws2_02_wal_tail_respects_limit`); S8-WS10-02 extended PARTIAL (driver session ping: `POST /api/v1/driver/ping`; `DriverPingRequest { session_token }`, `DriverPingResponse {status: "pong", session_token, pinged_at_ms}`; validates session exists via `driver_sessions` map → `401` if absent; returns `pinged_at_ms = now_unix_ms_u64()`; operator auth required; 2 tests `s8_ws10_02_driver_ping_invalid_session_returns_401` + `s8_ws10_02_driver_ping_valid_session_returns_pong`). SQL crate: **111 tests** (+3). Exec crate: **30 tests** (+2). Service: **341 tests total** (+4 new integration tests).

**2026-04-07 (session 28) progress note:** S3-WS1-06 extended PARTIAL (`has_group_by: bool` added to `SelectStatement`; detection via `up.contains("GROUP BY")` in `parse_tokens()` SELECT arm; 3 tests in new `group_by_detection_tests` module: `select_with_group_by_sets_has_group_by_true`, `select_without_group_by_has_group_by_false`, `select_group_by_with_having_sets_has_group_by_true` — sql crate **114 tests** +3); S3-WS1-06 exec extended (`Having { input: Box<LogicalPlan>, condition: String }` variant added after `Offset`; `plan_select()` inserts `after_having` stage between `after_agg` and `after_sort` — wraps in `Having` when `sel.having.is_some()`; `estimate_cost(Having)` → `estimated_rows = inner/2`, `relative_cost = inner + 1.0`, OLAP; `primary_table()` and `has_aggregation()` delegate to `input`; 2 tests `planner_having_produces_having_node` (SELECT * to avoid Project wrapper, asserts Having outermost + primary_table) + `cost_having_query_routes_to_olap` — exec crate **32 tests** +2); S2-WS2-02 extended PARTIAL (WAL segment list: `GET /api/v1/store/wal/segment/list`; `WalSegment {segment_id, is_active, record_count, start_sequence, end_sequence}`, `WalSegmentListResponse {segment_count, completed_segments, active_record_count, segments[]}`; handler builds one `WalSegment` per `checkpoint_count()` (is_active=false) plus one active segment from current `wal_records()`; operator auth; 2 tests `s2_ws2_02_wal_segment_list_empty_returns_one_active_segment` + `s2_ws2_02_wal_segment_list_shows_active_segment_record_count`); S8-WS10-02 extended PARTIAL (driver pool stats: `GET /api/v1/driver/pool/stats`; reuses existing `PoolStatsResponse` + `pool_stats_response()` helper; operator auth; 2 tests `s8_ws10_02_driver_pool_stats_fresh_state_shows_closed_circuit_breaker` + `s8_ws10_02_driver_pool_stats_requires_operator_auth` (uses `let Err(...)` pattern to avoid `Debug` bound on `PoolStatsResponse`)). SQL crate: **114 tests** (+3). Exec crate: **32 tests** (+2). Service: **345 tests total** (+4 new integration tests).

**2026-04-07 (session 29) progress note:** S3-WS1-06 extended PARTIAL (`has_order_by: bool` added to `SelectStatement`; detection via `up.contains("ORDER BY")` in `parse_tokens()` SELECT arm; 3 tests in new `order_by_detection_tests` module: `select_with_order_by_sets_has_order_by_true`, `select_without_order_by_has_order_by_false`, `select_order_by_with_limit_sets_has_order_by_true` (also asserts `limit = Some(10)`) — sql crate **117 tests** +3); S3-WS1-05 exec extended (`TopN { input, count: u64, order_by: String }` variant added after `Having`; `plan_select()` replaces the separate Sort+Limit stages with a single `TopN` node when both `!sel.order_by.is_empty() && sel.limit.is_some()`; pure ORDER BY still produces `Sort`, pure LIMIT still produces `Limit`; `estimate_cost(TopN)` → `rows.min(count)`, `*1.3`, OLTP; `primary_table()`/`has_aggregation()`/`is_read_only()` delegate to `input` via or-pattern; 2 tests `planner_topn_produced_when_order_by_and_limit` + `cost_topn_query_routes_to_oltp` — exec crate **34 tests** +2); S2-WS2-02 extended PARTIAL (WAL replay count: `GET /api/v1/store/wal/replay/count`; `WalReplayCountQuery {table_filter, op_filter}`, `WalReplayCountResponse {total_records, matched_count, table_filter, op_filter}`; filters by `key.starts_with(table_filter)` and op type inferred from `value == "__deleted__"`; operator auth; 2 tests `s2_ws2_02_wal_replay_count_empty_state_returns_zero` + `s2_ws2_02_wal_replay_count_filters_by_op`); S10-WS15-02 extended PARTIAL (CDC cursor list: `GET /api/v1/store/cdc/cursor/list`; `CdcCursorEntry {table_name, cursor_position}`, `CdcCursorListResponse {cursor_count, cursors[]}`; reads `cdc_cursors` HashMap, sorts entries by `table_name`; operator auth; 2 tests `s10_ws15_02_cdc_cursor_list_empty_on_fresh_state` + `s10_ws15_02_cdc_cursor_list_reflects_advanced_cursors`). SQL crate: **117 tests** (+3). Exec crate: **34 tests** (+2). Service: **349 tests total** (+4 new integration tests).

**2026-04-07 (session 30) progress note:** S3-WS1-04 extended PARTIAL (`has_having: bool` added to `SelectStatement`; `HAVING` keyword detected via `up.contains("HAVING")` in `parse_tokens()` SELECT arm; 3 tests in new `having_flag_tests` module: `select_with_having_sets_has_having_true`, `select_without_having_has_having_false`, `select_having_also_sets_has_group_by_true` — sql crate **120 tests** +3); S3-WS1-05 exec extended (`Subquery { input: Box<LogicalPlan> }` variant added after `TopN`; `plan_select()` wraps outermost node in `Subquery` when `sel.has_subquery`; `estimate_cost(Subquery)` → inner rows, `inner.relative_cost + 2.0`, Hybrid; `primary_table()`/`has_aggregation()` delegate through `input`; 2 tests `planner_subquery_produces_subquery_node` (asserts Subquery outermost + primary_table = "orders") + `cost_subquery_routes_to_hybrid` — exec crate **36 tests** +2); S2-WS2-04 extended PARTIAL (row store delete-by-key: `POST /api/v1/store/rows/delete`; `RowDeleteRequest { key: String }`, `RowDeleteResponse { status, key, deleted: bool }`; checks key existence via `scan_at_snapshot` + `iter().any(|(k,_)| *k == req.key)`, deletes via `rs.delete(xid, &req.key)` + WAL tombstone `__deleted__`; requires operator auth; 2 tests `s2_ws2_04_row_delete_existing_key_returns_deleted_true` + `s2_ws2_04_row_delete_missing_key_returns_deleted_false`); S5-WS4-03 extended PARTIAL (ingest schema list: `GET /api/v1/ingest/schema/list`; `IngestSchemaListQuery { format: Option<String> }`, `IngestSchemaListResponse { status, format_filter, connector_count, entries[] }`; iterates csv+json connector maps, filters by `format` query param when provided; requires `require_ingest_runtime_privilege`; 2 tests `s5_ws4_03_ingest_schema_list_no_filter_returns_all_formats` + `s5_ws4_03_ingest_schema_list_csv_filter_excludes_json`). SQL crate: **120 tests** (+3). Exec crate: **36 tests** (+2). Service: **353 tests total** (+4 new integration tests).

**2026-04-07 (session 31) progress note:** S3-WS1-07 new PARTIAL (`has_in_list: bool` added to `SelectStatement`; `IN (` detected via `up.contains(" IN (") && !up.contains("(SELECT")` in `parse_tokens()` SELECT arm — excludes subquery form so `has_subquery` stays exclusive; 3 tests in new `in_list_tests` module: `select_with_in_predicate_sets_has_in_list_true`, `select_with_in_list_string_values`, `plain_select_has_in_list_is_false` — sql crate **123 tests** +3); S3-WS1-07 exec extended (`InList { input: Box<LogicalPlan> }` variant added after `Union`; `plan_select()` wraps outermost in `InList` when `sel.has_in_list`; `estimate_cost(InList)` → `rows * 0.8`, `+0.5`, OLTP; `primary_table()`/`has_aggregation()` delegate; 2 tests `planner_in_list_select_produces_in_list_node` + `cost_in_list_query_routes_to_oltp` — exec crate **38 tests** +2); S5-WS4-03 extended PARTIAL (ingest format auto-detection: `POST /api/v1/ingest/format/detect`; `IngestFormatDetectRequest { sample_data }`/`IngestFormatDetectResponse { detected_format, confidence, field_count }`; JSON prefix detect, CSV comma detect, field count via object keys or comma split; operator auth; 2 tests `s5_ws4_03_ingest_format_detect_csv_sample` + `s5_ws4_03_ingest_format_detect_json_sample`); S7-WS6-01 new PARTIAL (Raft vote stats: `GET /api/v1/cluster/raft/vote/stats`; `RaftVoteStatsResponse { status, current_term, total_votes_granted: 0, total_votes_rejected: 0 }`; scaffold — vote accumulation not yet in `RaftNode`; operator auth; 2 tests `s7_ws6_01_raft_vote_stats_fresh_state_shows_zero_counts` + `s7_ws6_01_raft_vote_stats_reflects_current_term`). SQL crate: **123 tests** (+3). Exec crate: **38 tests** (+2). Service: **357 tests total** (+4 new integration tests).

**2026-04-07 (session 32) progress note:** S3-WS1-08 new PARTIAL (`has_between: bool` added to `SelectStatement`; `BETWEEN` detected via `up.contains(" BETWEEN ")` in `parse_tokens()` SELECT arm; 3 tests in new `between_tests` module: `select_with_between_predicate_sets_has_between_true`, `select_with_date_between_sets_has_between_true`, `plain_select_has_between_is_false` — sql crate **126 tests** +3); S3-WS1-08 exec extended (`Between { input: Box<LogicalPlan> }` variant added after `InList`; `plan_select()` wraps in `Between` when `sel.has_between`; `estimate_cost(Between)` → `rows * 0.6`, `+0.4`, OLTP; `primary_table()`/`has_aggregation()` delegate; 2 tests `planner_between_select_produces_between_node` + `cost_between_query_routes_to_oltp` — exec crate **40 tests** +2); S5-WS4-04 new PARTIAL (`POST /api/v1/ingest/connector/validate`; validates `connector_id` non-empty, `format` in {json,csv,parquet,excel}, `config_json` is valid JSON; `IngestConnectorValidateRequest/Response`; operator auth; 2 tests `s5_ws4_04_ingest_connector_validate_json_format` + `s5_ws4_04_ingest_connector_validate_unknown_format_is_invalid`); S2-WS2-03 new PARTIAL (`GET /api/v1/store/wal/mutations`; `WalMutationsQuery { limit }`, `WalMutationRecord { sequence, key, value }`, `WalMutationsResponse { mutation_count, limit_applied, mutations[] }`; returns last N WAL records, default limit 100, clamped 1–1000; operator auth; 2 tests `s2_ws2_03_wal_mutations_empty_state_returns_empty` + `s2_ws2_03_wal_mutations_limit_applied`). SQL crate: **126 tests** (+3). Exec crate: **40 tests** (+2). Service: **361 tests total** (+4 new integration tests).

**2026-04-07 (session 33) progress note:** S3-WS1-09 new PARTIAL (`has_like: bool` added to `SelectStatement`; `LIKE`/`ILIKE` detected via `up.contains(" LIKE ") || up.contains(" ILIKE ")` in `parse_tokens()` SELECT arm; 3 tests in new `like_tests` module: `select_with_like_predicate_sets_has_like_true`, `select_with_ilike_predicate_sets_has_like_true`, `plain_select_has_like_is_false` — sql crate **129 tests** +3); S3-WS1-09 exec extended (`Like { input }` variant added after `Between`; `plan_select()` wraps in `Like` when `sel.has_like`; `estimate_cost(Like)` → `rows * 0.7`, `+1.2`, **OLAP** path since LIKE requires full-scan; `primary_table()`/`has_aggregation()` delegate; 2 tests `planner_like_select_produces_like_node` + `cost_like_query_routes_to_olap` — exec crate **42 tests** +2); S5-E4A-01 extended PARTIAL (connector update: `POST /api/v1/connectors/update`; `ConnectorUpdateRequest { connector_id, version?, signed? }`/`ConnectorUpdateResponse { updated: bool }`; mutates matching registry entry; operator auth; 2 tests `s5_e4a_01_connector_update_existing_changes_version` + `s5_e4a_01_connector_update_missing_returns_updated_false`); S2-WS2-02 extended PARTIAL (WAL checkpoint history: `GET /api/v1/store/wal/checkpoint/history`; `WalCheckpointEntry { checkpoint_id, record_count_at_checkpoint }`/`WalCheckpointHistoryResponse { total_checkpoints, entries[] }`; one entry per `checkpoint_count()`; operator auth; 2 tests `s2_ws2_02_wal_checkpoint_history_empty_on_fresh_state` + `s2_ws2_02_wal_checkpoint_history_reflects_checkpoint_count`). SQL crate: **129 tests** (+3). Exec crate: **42 tests** (+2). Service: **365 tests total** (+4 new integration tests).
---

## 7) How to maintain this file

- After each sprint: flip tasks `TODO` → `DONE`, add discovered gaps.  
- Link evidence: `tests/kpi/results/...` and `cargo test -p ...` commands.  
- When stub crates are removed or implemented, update **Section 2** and **G-10**.  

---

*Generated for planning; aligns with `reference/voltnuerongrid-db-design.md` and `reference/voltnuerongrid-ws.md` checklist themes.*
